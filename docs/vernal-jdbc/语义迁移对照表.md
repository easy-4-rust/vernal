<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# spring-jdbc → vernal-jdbc 语义迁移对照表
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-jdbc` |
| 来源基线 | `org.springframework.jdbc` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 236 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-jdbc/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 236 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 本模块必须覆盖：连接生命周期、执行模板、参数绑定、结果映射、批处理、异常翻译、事务同步、LOB 与元数据。
- 必须记录入口、协作对象、回调顺序、错误传播、资源释放以及异步取消/背压语义。
- 接口相似不能代替行为证据；每个关键语义域都要有正常、失败和边界测试。
<!-- current-migration-contract-end -->

> 调用链由 Spring CodeGraph 核对。

| Spring 调用链 | Rust 合同 | 状态 |
|---|---|---|
| `JdbcTemplate#execute → DataSourceUtils#getConnection` | 从事务上下文复用连接，否则从 pool 获取 | `MISSING` |
| `PreparedStatementCreator → callback.doInPreparedStatement` | 设置参数、超时与 fetch 配置后调用 callback | `MISSING` |
| `query → executeQuery → ResultSetExtractor` | 流/行映射保持顺序并在所有路径清理结果集 | `MISSING` |
| `SQLException → close statement/release connection → translateException` | 先释放资源再翻译，保留 task/sql/cause | `MISSING` |
| finally → cleanup parameters/statement/connection | 成功和异常路径都执行幂等清理 | `MISSING` |

异步 sqlx 可替换底层驱动，但不能省略上述框架合同。

<!-- detailed-current-start -->
## vernal-jdbc 语义边界

语义等价以可观察行为为准，但不能借“功能相似”消除源对象边界。

| 语义域 | Rust 目标表达 | 必须保留 | 验收证据 |
|---|---|---|---|
| 连接生命周期 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 执行模板 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 参数绑定 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 结果映射 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 批处理 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 异常翻译 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 事务同步 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| LOB 与元数据 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |

## 对象簇职责

| 目标目录 | 数量 | 代表对象 | 当前状态 |
|---|---:|---|---|
| `config` | 5 | `DatabasePopulatorConfigUtils`、`EmbeddedDatabaseBeanDefinitionParser`、`InitializeDatabaseBeanDefinitionParser`、`JdbcNamespaceHandler`、`SortedResourcesFactoryBean` | MISSING:5 |
| `core` | 42 | `AggregatedBatchUpdateException`、`ArgumentPreparedStatementSetter`、`ArgumentTypePreparedStatementSetter`、`BatchPreparedStatementSetter`、`BeanPropertyRowMapper` | MISSING:42 |
| `core/metadata` | 22 | `CallMetaDataContext`、`CallMetaDataProvider`、`CallMetaDataProviderFactory`、`CallParameterMetaData`、`Db2CallMetaDataProvider` | MISSING:22 |
| `core/namedparam` | 12 | `AbstractSqlParameterSource`、`BeanPropertySqlParameterSource`、`EmptySqlParameterSource`、`MapSqlParameterSource`、`NamedParameterJdbcDaoSupport` | MISSING:12 |
| `core/simple` | 8 | `AbstractJdbcCall`、`AbstractJdbcInsert`、`DefaultJdbcClient`、`JdbcClient`、`SimpleJdbcCall` | MISSING:8 |
| `core/support` | 9 | `AbstractInterruptibleBatchPreparedStatementSetter`、`AbstractLobCreatingPreparedStatementCallback`、`AbstractLobStreamingResultSetExtractor`、`AbstractSqlTypeValue`、`JdbcBeanDefinitionReader` | MISSING:9 |
| `crate 根目录` | 8 | `BadSqlGrammarException`、`CannotGetJdbcConnectionException`、`IncorrectResultSetColumnCountException`、`InvalidResultSetAccessException`、`JdbcUpdateAffectedIncorrectNumberOfRowsException` | MISSING:8 |
| `datasource` | 20 | `AbstractDataSource`、`AbstractDriverBasedDataSource`、`ConnectionHandle`、`ConnectionHolder`、`ConnectionProxy` | MISSING:20 |
| `datasource/embedded` | 17 | `AbstractEmbeddedDatabaseConfigurer`、`ConnectionProperties`、`DataSourceFactory`、`DerbyEmbeddedDatabaseConfigurer`、`EmbeddedDatabase` | MISSING:17 |
| `datasource/init` | 11 | `CannotReadScriptException`、`CompositeDatabasePopulator`、`DataSourceInitializer`、`DatabasePopulator`、`DatabasePopulatorUtils` | MISSING:11 |
| `datasource/lookup` | 8 | `AbstractRoutingDataSource`、`BeanFactoryDataSourceLookup`、`DataSourceLookup`、`DataSourceLookupFailureException`、`IsolationLevelDataSourceRouter` | MISSING:8 |
| `object` | 13 | `BatchSqlUpdate`、`GenericSqlQuery`、`GenericStoredProcedure`、`MappingSqlQuery`、`MappingSqlQueryWithParameters` | MISSING:13 |
| `support` | 21 | `AbstractFallbackSQLExceptionTranslator`、`CustomSQLErrorCodesTranslation`、`CustomSQLExceptionTranslatorRegistrar`、`CustomSQLExceptionTranslatorRegistry`、`DatabaseMetaDataCallback` | MISSING:21 |
| `support/incrementer` | 22 | `AbstractColumnMaxValueIncrementer`、`AbstractDataFieldMaxValueIncrementer`、`AbstractIdentityColumnMaxValueIncrementer`、`AbstractSequenceMaxValueIncrementer`、`DataFieldMaxValueIncrementer` | MISSING:22 |
| `support/lob` | 7 | `AbstractLobHandler`、`DefaultLobHandler`、`LobCreator`、`LobHandler`、`PassThroughBlob` | MISSING:7 |
| `support/rowset` | 4 | `ResultSetWrappingSqlRowSet`、`ResultSetWrappingSqlRowSetMetaData`、`SqlRowSet`、`SqlRowSetMetaData` | MISSING:4 |
| `support/xml` | 7 | `Jdbc4SqlXmlHandler`、`SqlXmlFeatureNotImplementedException`、`SqlXmlHandler`、`SqlXmlValue`、`XmlBinaryStreamProvider` | MISSING:7 |

## 目标调用链

```mermaid
flowchart LR
    A["连接生命周期"] --> B["执行模板"]
    B --> C["参数绑定"]
    C --> D["结果映射"]
    D --> E["批处理"]
    E --> F["结果/错误/释放"]
    F -. 失败证据 .-> G["台账状态降级"]
```

入口、适配、执行、回调和清理都必须可观察；只验证最终返回值不足以证明顺序、取消或释放正确。

## Java 到 Rust 技术语义

| Java 机制 | Rust 映射 | 验证重点 |
|---|---|---|
| nullable | `Option<T>` | `None` 与空值不得混同 |
| checked exception | `thiserror` + `Result<T, E>` | 分类、cause 和上下文 |
| synchronized/并发容器 | `Arc<RwLock<_>>`/`DashMap` | 竞争、可见性和死锁 |
| CompletableFuture | `Future`/`JoinHandle` | 完成、失败、取消和超时 |
| Reactor Mono | `async fn -> Result<T, E>` | 单值、空结果和错误 |
| Reactor Flux | `Stream<Item=Result<T,E>>` | 背压、顺序、取消和终止 |
| ServiceLoader | `inventory` + registry | 自动注册和确定性 |
| JVM/字节码 | 有证据才可 `PLATFORM_NA` | 困难不等于不适用 |

## 语义测试矩阵

| 语义域 | 正常场景 | 失败/边界 | 观察点 |
|---|---|---|---|
| 连接生命周期 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 执行模板 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 参数绑定 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 结果映射 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 批处理 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 异常翻译 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 事务同步 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| LOB 与元数据 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |

## 语义完成清单

- [ ] 对象职责和协作边界明确。
- [ ] Javadoc 前置、后置和异常语义译为中文注释。
- [ ] 关键链路有正常、失败和边界测试。
- [ ] 异步能力覆盖完成、错误、取消、超时与背压。
- [ ] 资源型对象覆盖获取、复用和释放。
- [ ] 依赖复用有固定提交、精确符号和集成测试。
- [ ] JVM 专属能力有不可迁移证据。
- [ ] 状态严格使用验收规范定义。
<!-- detailed-current-end -->
