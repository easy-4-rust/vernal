<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-log 对象级对照表
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-log` |
| 来源基线 | 文档型/依赖适配模块；以显式规划对象和固定依赖证据为边界 |
| 当前对象边界 | 5 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `docs/vernal-log 中声明的目标 crate/适配层` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 5 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 必须覆盖全部对象的 FQN、源路径、预期路径、当前路径、状态和可定位证据。
- 顶部当前事实优先于历史设计附录；附录中的旧统计和旧完成标记不参与验收。
- 依赖复用必须记录 crate、固定提交、精确符号和本地集成测试。
<!-- current-migration-contract-end -->

| Java FQN | 预期 Rust 路径 | 状态 | 证据 |
|---|---|---|---|
| `org.springframework.core.log.CompositeLog` | `core/log/composite_log.rs` | `MISSING` | 无同名文件 |
| `org.springframework.core.log.LogAccessor` | `core/log/log_accessor.rs` | `MISSING` | 无同名文件 |
| `org.springframework.core.log.LogDelegateFactory` | `core/log/log_delegate_factory.rs` | `MISSING` | 无同名文件 |
| `org.springframework.core.log.LogFormatUtils` | `core/log/log_format_utils.rs` | `MISSING` | 无同名文件 |
| `org.springframework.core.log.LogMessage` | `core/log/log_message.rs` | `MISSING` | 无同名文件 |

`tracing` 复用状态目前为 `UNVERIFIED`，没有精确到上述对象的依赖符号与集成测试。

<!-- detailed-current-start -->
## vernal-log 对象边界

本模块没有独立 Spring 源码扫描根；下表是显式本地对象或对象族，`PLANNED`/`UNVERIFIED` 不计入完成。

## 对象职责与证据

| 对象 | 预期路径 | 职责 | 状态 | 必需证据 |
|---|---|---|---|---|
| `CompositeLog` | `core/log/composite_log.rs` | 延迟消息 | `MISSING` | 正常、失败、顺序与释放测试 |
| `LogAccessor` | `core/log/log_accessor.rs` | 复合级别委派 | `MISSING` | 正常、失败、顺序与释放测试 |
| `LogDelegateFactory` | `core/log/log_delegate_factory.rs` | 结构化字段 | `MISSING` | 正常、失败、顺序与释放测试 |
| `LogFormatUtils` | `core/log/log_format_utils.rs` | tracing 桥接 | `MISSING` | 正常、失败、顺序与释放测试 |
| `LogMessage` | `core/log/log_message.rs` | 格式限制 | `MISSING` | 正常、失败、顺序与释放测试 |

## 状态和依赖边界

| 判定 | 允许条件 | 当前规则 |
|---|---|---|
| `IMPLEMENTED` | 文件、逻辑、中文注释和测试齐全 | 未审计不得声明 |
| `DEPENDENCY_REUSED` | crate/提交/符号/集成测试齐全 | 仅生态相似仍未完成 |
| `PLATFORM_NA` | JVM 或字节码不可迁移证据 | 实现困难不构成证据 |
| `PARTIAL` | 有真实逻辑但语义不全 | 必须登记缺口 |
| `PLANNED`/`UNVERIFIED` | 设计存在但证据不足 | 不计入完成 |

## 对象协作链

```mermaid
flowchart LR
    A["入口合同"] --> B["适配对象"]
    B --> C["依赖或运行时"]
    C --> D["结果/错误翻译"]
    D --> E["资源释放与观测"]
    E -. 验收证据 .-> F["台账状态"]
```

## 集成测试矩阵

| 语义域 | 正常场景 | 失败场景 | 证据 |
|---|---|---|---|
| 延迟消息 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |
| 复合级别委派 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |
| 结构化字段 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |
| tracing 桥接 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |
| 格式限制 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |
| 错误链记录 | 经适配层完成真实调用 | 依赖错误、取消或释放失败 | 类型化错误、顺序、状态和测试路径 |

## 对象完成清单

- [ ] 一个对象一个 snake_case 文件。
- [ ] 主类型 PascalCase，方法参数 snake_case。
- [ ] `mod.rs` 只声明并重导出。
- [ ] 对象及公开方法有中文来源/设计注释。
- [ ] 依赖符号和版本固定可追溯。
- [ ] 适配层保留原始错误 cause。
- [ ] 资源、事务、取消和释放有测试。
- [ ] 对象表与路线图使用同一对象集合。
<!-- detailed-current-end -->
