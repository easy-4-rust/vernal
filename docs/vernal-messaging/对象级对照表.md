# spring-messaging → vernal-messaging 对象级对照表

> 基线：Spring Framework 7.0.8 `spring-messaging`。本表按包和对象族列出迁移落点；协议、Reactive、模板与注解不再以“排除”表示，而以 feature/独立 crate 记录。
> 状态：✅ 已实现｜⬜ 待实现｜🔶 Rust 形态不同但需保持语义｜🧩 feature/companion crate｜🆕 Rust 新增｜🔌 外部桥接（不进核心）。

## 一、目标 crate 与统计口径

| 层 | 目标 | 是否属于核心 |
|---|---|---:|
| `vernal-messaging` | Spring Messaging 核心合同与默认实现 | 是 |
| `vernal-messaging` `reactive` feature 或 `vernal-messaging-reactive` | Reactive handler、流、背压、取消 | 可选 companion |
| `vernal-stomp` | STOMP frame、session、broker、用户目的地 | 否，协议 companion |
| `vernal-rsocket` | RSocket service、metadata、交互模型 | 否，协议 companion |
| `vernal-messaging-tcp` | TCP transport、codec、连接生命周期 | 否，transport companion |
| `ddd4r-vernal-messaging` | ddd4r 事件、发布、消费、确认、生命周期桥接 | 否，外部集成 |

对象统计采用“Java 对象族 + Rust 落点”口径。一个 Rust 文件可以承载紧密相关对象，但不得因此丢失单项语义或测试 oracle。

## 二、根包 `org.springframework.messaging`

| Java 对象族 | Rust 落点 | 状态 | 必须保留的语义 |
|---|---|---:|---|
| `Message<T>` | `message.rs` | ⬜ | payload、headers、泛型边界 |
| `MessageHeaders`、标准 header 常量 | `message_headers.rs` | ⬜ | ID、timestamp、contentType、reply/error channel、不可变视图 |
| `MessageChannel` | `message_channel.rs` | ⬜ | send、timeout、返回成功/失败 |
| `PollableChannel` | `pollable_channel.rs` | ⬜ | receive、超时、空值/关闭语义 |
| `SubscribableChannel` | `subscribable_channel.rs` | ⬜ | subscribe、unsubscribe、订阅生命周期 |
| `MessageHandler` | `message_handler.rs` | ⬜ | 单消息处理、异常传播 |
| `ReactiveMessageHandler` | `reactive_message_handler.rs` 或 `reactive` feature | 🧩 | demand、取消、错误、完成 |
| `MessagingException` 族 | `messaging_error.rs` | ⬜ | failed message、cause、分类 |

## 三、`support` 消息与通道对象族

| Java 对象 | Rust 落点 | 状态 | 备注 |
|---|---|---:|---|
| `MessageBuilder` | `message_builder.rs` | ⬜ | set/replace/absent/copy headers |
| `MessageHeaderAccessor`、`NativeMessageHeaderAccessor` | `message_header_accessor.rs` | ⬜ | mutable 构建视图与 native headers |
| `GenericMessage` | `generic_message.rs` | ⬜ | 标准 payload + headers 实现 |
| `ErrorMessage` | `error_message.rs` | ⬜ | 错误作为 payload，保留 failed message |
| `AbstractMessageChannel` | `abstract_message_channel.rs` | 🔶 | 默认发送流程与拦截器模板 |
| `AbstractSubscribableChannel` | `abstract_subscribable_channel.rs` | 🔶 | 订阅管理，不隐含 broker 消费 |
| `ExecutorSubscribableChannel` | `executor_subscribable_channel.rs` | ⬜ | executor 调度、顺序/并发策略 |
| `DirectChannel`、`PublishSubscribeChannel`、`QueueChannel`、`PriorityChannel` | 对应独立 channel 文件 | ⬜ | 同步直达、广播、队列、优先级语义 |
| `ChannelInterceptor` | `channel_interceptor.rs` | ⬜ | send/receive 前后及 completion 回调 |
| `ExecutorChannelInterceptor` | `executor_channel_interceptor.rs` | ⬜ | executor 上下文回调 |
| `InterceptableChannel` | `interceptable_channel.rs` | ⬜ | add/remove/set interceptor |
| `MessageHandlingRunnable` | `message_handling_runnable.rs` | ⬜ | 调度包装和异常交付 |

## 四、`converter` 对象族

| Java 对象族 | Rust 落点 | 状态 | 语义要求 |
|---|---|---:|---|
| `MessageConverter`、`SmartMessageConverter` | `message_converter.rs` | ⬜ | from/to、conversion hint、失败分类 |
| `AbstractMessageConverter` | `abstract_message_converter.rs` | ⬜ | supported MIME、协商、fallback |
| `CompositeMessageConverter` | `composite_message_converter.rs` | ⬜ | 顺序委托与未转换结果 |
| `StringMessageConverter` | `string_message_converter.rs` | ⬜ | text/plain 与字符编码 |
| `ByteArrayMessageConverter` | `byte_array_message_converter.rs` | ⬜ | bytes 与 octet-stream |
| Jackson/Gson/JSON-B/Kotlin JSON | `json_message_converter.rs` 或独立 feature | 🔶 | 可用 serde 实现，但 content type、序列化错误和配置需等价 |
| `ProtobufMessageConverter` | `protobuf` feature / `vernal-messaging-protobuf` | 🧩 | 不得永久删除 protobuf 语义 |
| `MarshallingMessageConverter` | `marshalling` feature / companion crate | 🧩 | XML/自定义 marshaller 合同 |

## 五、`core` 模板对象族

| Java 对象 | Rust 落点 | 状态 | 语义 |
|---|---|---:|---|
| `MessageSendingOperations` | `message_sending_operations.rs` | ⬜ | send、convertAndSend、post processor |
| `MessageReceivingOperations` | `message_receiving_operations.rs` | ⬜ | receive、receiveAndConvert |
| `MessageRequestReplyOperations` | `message_request_reply_operations.rs` | ⬜ | reply channel、timeout、错误 |
| `GenericMessagingTemplate` | `messaging_template.rs` | ⬜ | 组合发送/接收/请求回复 |
| `AbstractMessageSendingTemplate` 等基类 | 对应 template 文件 | 🔶 | 保留默认流程和 hook |
| `DestinationResolver` | `destination_resolver.rs` | ⬜ | 目的地解析、解析失败 |
| `BeanFactoryMessageChannelDestinationResolver` | `vernal` adapter 或核心注册表适配 | 🔶 | 不复制 BeanFactory，保留解析合同 |
| `MessagePostProcessor` | `message_post_processor.rs` | ⬜ | 发送前/后消息变换时机 |

## 六、`handler` 与注解对象族

| Java 对象族 | Rust 落点 | 状态 | 语义 |
|---|---|---:|---|
| `HandlerMethod`、`InvocableHandlerMethod` | `handler_method.rs`、`invocable_handler_method.rs` | 🔶 | 方法选择、参数绑定、返回值、异常 |
| `HandlerMethodArgumentResolver` 及 Composite | `handler_method_argument_resolver.rs` | ⬜ | payload、header、message、destination variable |
| `HandlerMethodReturnValueHandler` 及 Composite | `handler_method_return_value_handler.rs` | ⬜ | reply、channel、void、异步结果 |
| `MessageCondition`、`CompositeMessageCondition` | `message_condition.rs` | ⬜ | combine、matching、compare |
| `DestinationPatternsMessageCondition` | `destination_patterns_message_condition.rs` | ⬜ | Ant 模式、精确匹配、排序 |
| `@MessageMapping` | `vernal-macros` `message_mapping` | 🧩 | 编译期注册映射 |
| `@Payload`、`@Header`、`@Headers` | `vernal-macros` 参数属性 | 🧩 | 参数来源与缺失策略 |
| `@DestinationVariable`、`@SendTo` | `vernal-macros` | 🧩 | 路径变量与回复目的地 |
| `@MessageExceptionHandler` | `vernal-macros` | 🧩 | 异常映射与优先级 |

## 七、协议与 Reactive 对象族

| Spring 包 | Rust 落点 | 状态 | 不能丢失的语义 |
|---|---|---:|---|
| `simp`、`simp.stomp` | `vernal-stomp` | 🧩 | STOMP 1.x frame、CONNECT、SUBSCRIBE、SEND、ACK/NACK、receipt、heartbeat、session、simple broker、用户目的地 |
| `rsocket` | `vernal-rsocket` | 🧩 | request-response、fire-and-forget、request-stream、channel、metadata、resume/error |
| `tcp` | `vernal-messaging-tcp` | 🧩 | 连接、帧边界、编解码、超时、关闭、重连、背压 |
| `handler.invocation.reactive` | `vernal-messaging-reactive` | 🧩 | Publisher/Subscriber、需求传播、取消、错误/完成 |
| `handler.annotation.reactive` | `vernal-messaging-reactive` + macros | 🧩 | Reactive 参数/返回值解析与映射 |

## 八、Rust 新增对象

| Rust 对象 | 落点 | 原因 |
|---|---|---|
| `ReactiveExecution`、`Demand` | reactive companion | 显式表达 Java Reactor 隐含合同 |
| `ProtocolFrame`、`SessionState` | STOMP/RSocket/TCP companions | 将协议状态机变成可测 Rust 类型 |
| `SettlementPort`、`DeliveryContext` | `ddd4r-vernal-messaging` | 与 interceptor 分离的确认/消费合同 |
| `PublisherSendAdapter` | `ddd4r-vernal-messaging` | 仅适配发布，不伪造订阅能力 |

## 九、ddd4r 外部桥接对象

| ddd4r 对象 | 外部适配落点 | 状态 | 明确边界 |
|---|---|---:|---|
| `MqEvent`/`EventEnvelope` | `event_message_adapter.rs` | 🔌 | 转换为 Message，不改核心 trait |
| `MessagePublisher` | `publisher_send_adapter.rs` | 🔌 | 只提供 send/publish；不是 `SubscribableChannel` |
| `MessageHandler`/`DeliveryHandler` | `delivery_handler_adapter.rs` | 🔌 | delivery context 与处理结果显式映射 |
| `Acknowledgment` | `acknowledgment_adapter.rs` | 🔌 | Ack/Nack/Requeue 状态机，不映射为 interceptor |
| `ConsumerEngine` | `consumer_engine_runtime.rs` | 🔌 | 外部消费运行时与生命周期 |
| `TagExpression` | `tag_condition_adapter.rs` | 🔌 | 仅做显式路由转换，记录语义差异 |

## 十、文件与统计规则

目标文件数不再用“28+”代替完整范围。核心、协议、Reactive、外部桥接分别统计；对象合并须在本表出现，feature/companion 必须有测试入口。四文档统一采用：Spring 7.0.8、S0-S13、状态图例、核心无 ddd4r、`ddd4r-vernal-messaging` 外置桥接。
