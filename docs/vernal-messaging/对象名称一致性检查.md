<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-messaging 对象名称一致性检查
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-messaging` |
| 来源基线 | `org.springframework.messaging` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 222 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-messaging/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 2 |
| `MISSING` | 219 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 1 |

### 本文档执行责任

- 必须逐对象核对 Java 名称、snake_case 文件名、末两层目录、当前路径和公开主类型。
- 同名但目录不符一律标为 `MISPLACED`；文件存在但注释或测试不足标为 `UNVERIFIED`。
- 不能用“已合并”“已改名”或兼容重导出消除应有对象文件。
<!-- current-migration-contract-end -->

| 项目 | 当前事实 |
|---|---:|
| Spring 业务对象 | 222 |
| 严格已处理 | 0 |
| `MISPLACED` | 2 |
| `MISSING` | 219 |
| `UNVERIFIED` | 1 |

权威逐项证据见[自动对象审计](../migration-audit/vernal-messaging.md)。

主要问题：

- `message.rs` 同时定义 `Message` 与 `GenericMessage`。
- `channel.rs` 同时定义多个 channel/handler/error 对象。
- `default_simp_user_registry.rs` 同时定义 user/session/subscription/registry 多个对象。
- `Message` 虽在预期路径，但缺完整 Java 来源注释与测试证据，仍为 `UNVERIFIED`。
- 旧文档把协议拆到 companion crate 后直接视为覆盖，违反精确依赖复用规则。

> 原对象分组、人工说明和迁移背景已完整保存在[对象级对照表的历史设计附录](对象级对照表.md#历史设计附录不参与当前状态)；当前状态以自动生成对象表为准。

---

<!-- restored-detail-from-head: dd20300d16a09200bd8a379ff14db1e2da99b67c -->

## 原详细文档（完整保留）

> 以下正文完整恢复自 Vernal 提交 `dd20300d16a09200bd8a379ff14db1e2da99b67c`。其中历史对象数量、完成状态、
> 路径算法和依赖替代结论如与本文顶部或自动对象台账冲突，以顶部当前结论和
> `docs/migration-audit/` 为准；其 API、设计背景、阶段拆解和测试说明继续保留。

# vernal-messaging 对象名称一致性检查

> 检查时间：2026-07-27｜基线：Spring Framework 7.0.8 `spring-messaging`
> 本检查验证四文档的名称、术语、依赖边界和统计口径；它不把“Rust 文件同名”误当作语义完成。

## 一、统一命名规则

| 层级 | 规则 | 示例 |
|---|---|---|
| Java 类型映射 | PascalCase → snake_case | `MessageHeaderAccessor` → `message_header_accessor.rs` |
| Rust 类型 | PascalCase | `MessageChannel`、`GenericMessagingTemplate` |
| Rust 方法 | snake_case | `handle_message`、`send_and_receive` |
| 常量 | UPPER_SNAKE_CASE | `INDEFINITE_TIMEOUT`、`CONTENT_TYPE` |
| 协议 crate | `vernal-*` | `vernal-stomp`、`vernal-rsocket` |
| 外部集成 crate | `ddd4r-vernal-messaging` | 只放桥接，不放核心实现 |

对象名称一致性要求：每个 Spring 对象必须有唯一 Rust 落点记录；合并实现要列出全部对象；feature/companion crate 要写明路径；外部 ddd4r 类型不能冒充 Spring 对偶。

## 二、四文档一致性检查表

| 检查项 | 统一结论 | 路线图 | 对象表 | 语义表 | 本表 |
|---|---|---:|---:|---:|---:|
| Spring 基线 | 7.0.8 | ✅ | ✅ | ✅ | ✅ |
| 完整迁移原则 | 功能语义不排除 STOMP/RSocket/TCP/Reactive/模板 | ✅ | ✅ | ✅ | ✅ |
| 核心依赖 | `vernal-messaging` 不依赖 ddd4r | ✅ | ✅ | ✅ | ✅ |
| 桥接位置 | `ddd4r-vernal-messaging` 外部 crate | ✅ | ✅ | ✅ | ✅ |
| Ack 定义 | 不等于 ChannelInterceptor | ✅ | ✅ | ✅ | ✅ |
| Publisher 定义 | 不天然是 SubscribableChannel | ✅ | ✅ | ✅ | ✅ |
| ConsumerEngine | 不属于核心 | ✅ | ✅ | ✅ | ✅ |
| 阶段 | S0-S13 | ✅ | 引用 | 引用 | ✅ |
| 测试 oracle | 用户可观察语义 | ✅ | 按对象 | 按语义 | ✅ |

## 三、核心对象名称清单

| Spring 名称 | Rust 名称/落点 | 一致性 |
|---|---|---:|
| `Message` | `Message` / `message.rs` | ✅ |
| `MessageHeaders` | `MessageHeaders` / `message_headers.rs` | ✅ |
| `MessageBuilder` | `MessageBuilder` / `message_builder.rs` | ✅ |
| `MessageHeaderAccessor` | `MessageHeaderAccessor` / `message_header_accessor.rs` | ✅ |
| `GenericMessage` | `GenericMessage` / `generic_message.rs` | ✅ |
| `ErrorMessage` | `ErrorMessage` / `error_message.rs` | ✅ |
| `MessageChannel` | `MessageChannel` / `message_channel.rs` | ✅ |
| `SubscribableChannel` | `SubscribableChannel` / `subscribable_channel.rs` | ✅ |
| `PollableChannel` | `PollableChannel` / `pollable_channel.rs` | ✅ |
| `ChannelInterceptor` | `ChannelInterceptor` / `channel_interceptor.rs` | ✅ |
| `MessageHandler` | `MessageHandler` / `message_handler.rs` | ✅ |
| `ReactiveMessageHandler` | `ReactiveMessageHandler` / reactive companion | ✅ |
| `MessageConverter` | `MessageConverter` / `message_converter.rs` | ✅ |
| `GenericMessagingTemplate` | `GenericMessagingTemplate` / `messaging_template.rs` | ✅ |
| `DestinationResolver` | `DestinationResolver` / `destination_resolver.rs` | ✅ |
| `MessageCondition` | `MessageCondition` / `message_condition.rs` | ✅ |
| `DestinationPatternsMessageCondition` | 同名 / `destination_patterns_message_condition.rs` | ✅ |
| `@MessageMapping` | `message_mapping` macro | 🔶 Rust 宏形态 |

## 四、协议与 Reactive 名称清单

| Spring 包/对象族 | Rust 落点 | 一致性 |
|---|---|---:|
| `messaging.simp` / STOMP | `vernal-stomp` | 🔶 companion crate |
| `messaging.rsocket` | `vernal-rsocket` | 🔶 companion crate |
| `messaging.tcp` | `vernal-messaging-tcp` | 🔶 transport companion |
| `handler.invocation.reactive` | `vernal-messaging-reactive` | 🔶 async-stream 形态 |
| `handler.annotation.reactive` | reactive macros/registry | 🔶 编译期注册 |

检查结论：这些名称不是“排除项”；它们是必须完成的可选模块。若采用 feature 而非独立 crate，feature 名必须保持 `stomp`、`rsocket`、`tcp`、`reactive` 的可识别性，并在对象表登记。

## 五、ddd4r/ddd4j 名称与边界检查

| ddd4r 名称 | 正确位置 | 与 Spring 名称关系 | 禁止写法 |
|---|---|---|---|
| `MqEvent` / `EventEnvelope` | `ddd4r-vernal-messaging` | Message 的外部适配输入 | 核心实现 `MqEvent: Message` 并反向依赖 ddd4r |
| `MessagePublisher` | `PublisherSendAdapter` | 发送能力适配 | 直接称为 `SubscribableChannel` |
| `Acknowledgment` | `SettlementPort`/adapter | delivery settlement | 写成 `ChannelInterceptor` |
| `ConsumerEngine` | `consumer_engine_runtime` | 消费运行时 | 放入 `vernal-messaging` |
| `TagExpression` | `tag_condition_adapter` | 路由表达式适配 | 宣称与 Ant pattern 完全同构 |
| ddd4j headers | 外部映射命名空间 | 可映射 metadata | 覆盖 Spring 标准 header |

## 六、统计与术语门禁

1. 四文档只使用“核心 crate”“feature/companion crate”“外部桥接 crate”三种迁移位置。
2. 不再使用“STOMP/RSocket/TCP/Reactive 共 X 个不迁移”作为结论；正确表述是“在 S8-S11 由 feature 或 companion crate 实现”。
3. “Ack/确认”“拦截器”“发布”“订阅”“消费运行时”必须分别使用，不能互换。
4. 路线阶段固定为 S0-S13；任何新增阶段必须同步更新四文档。
5. 对象计数以对象清单为准，Rust 文件数只作工程规模指标，不能作为覆盖率。
6. 每个对象或对象族必须关联用户可观察 oracle；没有 oracle 的条目只能是规划状态。

## 七、检查结果

| 项目 | 结果 |
|---|---|
| 四文件名称 | 与用户批准范围完全一致 |
| 中文与表格 | 已统一 |
| S0-S13 | 已统一 |
| 完整模板体系 | 已纳入 |
| STOMP/RSocket/TCP/Reactive | 已纳入 feature/companion 路线 |
| ddd4r 核心依赖边界 | 已改为外部 `ddd4r-vernal-messaging` |
| Ack / interceptor | 已明确分离 |
| Publisher / SubscribableChannel | 已明确分离 |
| ConsumerEngine | 已明确不属于核心 |
| 测试 oracle | 四文档均有对应约束 |

最终命名结论：Spring 对象名称在 Rust 中保持可识别的一一落点；Rust 化只改变实现形态，不改变用户可观察语义。ddd4r 类型通过外部桥接命名，不污染 `vernal-messaging` 核心命名空间。
