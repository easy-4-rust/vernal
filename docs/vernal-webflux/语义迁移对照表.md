<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-webflux 语义迁移对照表
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-webflux` |
| 来源基线 | `org.springframework` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 252 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-webflux/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 252 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 本模块必须覆盖：异步分发、映射与适配、参数与返回值、Codec、背压取消、过滤器顺序、异常响应、WebClient。
- 必须记录入口、协作对象、回调顺序、错误传播、资源释放以及异步取消/背压语义。
- 接口相似不能代替行为证据；每个关键语义域都要有正常、失败和边界测试。
<!-- current-migration-contract-end -->

> CodeGraph 主链：`DispatcherHandler#handle` 按顺序查询 `HandlerMapping`，选择支持目标的
> `HandlerAdapter`，再由 `HandlerResultHandler` 写响应；preflight 和异常恢复是独立分支。

| Spring 语义 | Rust 目标 | 状态 |
|---|---|---|
| `Flux.concatMap(...).next()` 有序首匹配 | ordered async matcher | `PLANNED` |
| `Mono` 错误恢复 | `Result` + async error mapper | `PLANNED` |
| `Flux` body 与背压 | `Stream<Item=Result<...>>` | `PLANNED` |
| request cancellation | cancellation token + drop guard | runtime 层部分存在，未对象验收 |
| preflight 403 | CORS strategy + committed response | `PLANNED` |
| result handler selection | typed return-value registry | `PLANNED` |

异步语法等价不代表错误、取消、顺序和背压语义等价。

<!-- detailed-current-start -->
## vernal-webflux 语义边界

语义等价以可观察行为为准，但不能借“功能相似”消除源对象边界。

| 语义域 | Rust 目标表达 | 必须保留 | 验收证据 |
|---|---|---|---|
| 异步分发 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 映射与适配 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 参数与返回值 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| Codec | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 背压取消 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 过滤器顺序 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 异常响应 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| WebClient | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |

## 对象簇职责

| 目标目录 | 数量 | 代表对象 | 当前状态 |
|---|---:|---|---|
| `client/support` | 5 | `ClientResponseWrapper`、`NotFoundWebClientAdapterDecorator`、`WebClientAdapter`、`WebClientHttpServiceGroupAdapter`、`WebClientHttpServiceGroupConfigurer` | MISSING:5 |
| `function/client` | 23 | `ClientHttpObservationDocumentation`、`ClientRequest`、`ClientRequestObservationContext`、`ClientRequestObservationConvention`、`ClientResponse` | MISSING:23 |
| `function/server` | 23 | `ChangePathPatternParserVisitor`、`DefaultEntityResponseBuilder`、`DefaultHandlerStrategiesBuilder`、`DefaultRenderingResponseBuilder`、`DefaultServerRequest` | MISSING:23 |
| `method/annotation` | 39 | `AbstractMessageReaderArgumentResolver`、`AbstractMessageWriterResultHandler`、`AbstractNamedValueArgumentResolver`、`AbstractNamedValueSyncArgumentResolver`、`ApiVersionMethodArgumentResolver` | MISSING:39 |
| `reactive/accept` | 15 | `ApiVersionDeprecationHandler`、`ApiVersionResolver`、`ApiVersionStrategy`、`AsyncApiVersionResolver`、`DefaultApiVersionStrategy` | MISSING:15 |
| `reactive/config` | 15 | `ApiVersionConfigurer`、`BlockingExecutionConfigurer`、`CorsRegistration`、`CorsRegistry`、`DelegatingWebFluxConfiguration` | MISSING:15 |
| `reactive/function` | 5 | `BodyExtractor`、`BodyExtractors`、`BodyInserter`、`BodyInserters`、`UnsupportedMediaTypeException` | MISSING:5 |
| `reactive/handler` | 4 | `AbstractHandlerMapping`、`AbstractUrlHandlerMapping`、`SimpleUrlHandlerMapping`、`WebFluxResponseStatusExceptionHandler` | MISSING:4 |
| `reactive/resource` | 26 | `AbstractFileNameVersionStrategy`、`AbstractPrefixVersionStrategy`、`AbstractResourceResolver`、`CachingResourceResolver`、`CachingResourceTransformer` | MISSING:26 |
| `reactive/result` | 3 | `ExtendedWebExchangeDataBinder`、`HandlerResultHandlerSupport`、`SimpleHandlerAdapter` | MISSING:3 |
| `reactive/socket` | 5 | `CloseStatus`、`HandshakeInfo`、`WebSocketHandler`、`WebSocketMessage`、`WebSocketSession` | MISSING:5 |
| `result/condition` | 15 | `AbstractMediaTypeExpression`、`AbstractNameValueExpression`、`AbstractRequestCondition`、`CompositeRequestCondition`、`ConsumesRequestCondition` | MISSING:15 |
| `result/method` | 9 | `AbstractHandlerMethodMapping`、`HandlerMethodArgumentResolver`、`HandlerMethodArgumentResolverComposite`、`HandlerMethodArgumentResolverSupport`、`InvocableHandlerMethod` | MISSING:9 |
| `result/view` | 18 | `AbstractUrlBasedView`、`AbstractView`、`BindStatus`、`DefaultFragmentsRenderingBuilder`、`DefaultRendering` | MISSING:18 |
| `server/support` | 9 | `HandlerFunctionAdapter`、`RouterFunctionMapping`、`ServerRequestWrapper`、`ServerResponseResultHandler`、`SupportedVersionVisitor` | MISSING:9 |
| `server/upgrade` | 5 | `DefaultServerEndpointConfig`、`JettyCoreRequestUpgradeStrategy`、`JettyRequestUpgradeStrategy`、`ReactorNettyRequestUpgradeStrategy`、`StandardWebSocketUpgradeStrategy` | MISSING:5 |
| `socket/adapter` | 10 | `AbstractListenerWebSocketSession`、`AbstractWebSocketSession`、`ContextWebSocketHandler`、`JettyWebSocketHandlerAdapter`、`JettyWebSocketSession` | MISSING:10 |
| `socket/client` | 5 | `JettyWebSocketClient`、`ReactorNettyWebSocketClient`、`StandardWebSocketClient`、`TomcatWebSocketClient`、`WebSocketClient` | MISSING:5 |
| `socket/server` | 2 | `RequestUpgradeStrategy`、`WebSocketService` | MISSING:2 |
| `view/freemarker` | 4 | `FreeMarkerConfig`、`FreeMarkerConfigurer`、`FreeMarkerView`、`FreeMarkerViewResolver` | MISSING:4 |
| `view/script` | 5 | `RenderingContext`、`ScriptTemplateConfig`、`ScriptTemplateConfigurer`、`ScriptTemplateView`、`ScriptTemplateViewResolver` | MISSING:5 |
| `web/reactive` | 7 | `BindingContext`、`DispatchExceptionHandler`、`DispatcherHandler`、`HandlerAdapter`、`HandlerMapping` | MISSING:7 |

## 目标调用链

```mermaid
flowchart LR
    A["异步分发"] --> B["映射与适配"]
    B --> C["参数与返回值"]
    C --> D["Codec"]
    D --> E["背压取消"]
    E --> F["结果/错误/释放"]
    F -. 失败证据 .-> G["台账状态降级"]
```

入口、适配、执行、回调和清理都必须可观察；只验证最终返回值不足以证明顺序、取消或释放正确。

## Java 到 Rust 技术语义

| Java 机制 | Rust 映射 | 验证重点 |
|---|---|---|
| nullable | `Option<T>` | `None` 与空值不得混同 |
| checked exception | `thiserror` + `Result<T, E>` | 分类、cause 和上下文 |
| synchronized/并发容器 | `Arc<RwLock<_>>`/`DashMap` | 竞争、可见性和死锁 |
| CompletableFuture | `Future`/`JoinHandle` | 完成、失败、取消和超时 |
| Reactor Mono | `async fn -> Result<T, E>` | 单值、空结果和错误 |
| Reactor Flux | `Stream<Item=Result<T,E>>` | 背压、顺序、取消和终止 |
| ServiceLoader | `inventory` + registry | 自动注册和确定性 |
| JVM/字节码 | 有证据才可 `PLATFORM_NA` | 困难不等于不适用 |

## 语义测试矩阵

| 语义域 | 正常场景 | 失败/边界 | 观察点 |
|---|---|---|---|
| 异步分发 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 映射与适配 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 参数与返回值 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| Codec | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 背压取消 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 过滤器顺序 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 异常响应 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| WebClient | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |

## 语义完成清单

- [ ] 对象职责和协作边界明确。
- [ ] Javadoc 前置、后置和异常语义译为中文注释。
- [ ] 关键链路有正常、失败和边界测试。
- [ ] 异步能力覆盖完成、错误、取消、超时与背压。
- [ ] 资源型对象覆盖获取、复用和释放。
- [ ] 依赖复用有固定提交、精确符号和集成测试。
- [ ] JVM 专属能力有不可迁移证据。
- [ ] 状态严格使用验收规范定义。
<!-- detailed-current-end -->
