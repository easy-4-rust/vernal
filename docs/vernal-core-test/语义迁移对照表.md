<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# spring-core-test → vernal-core-test 语义迁移对照表
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-core-test` |
| 来源基线 | `` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 40 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-core-test/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 40 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 本模块必须覆盖：测试缓冲与流、反射断言、资源夹具、并发协调、失败诊断、跨平台确定性。
- 必须记录入口、协作对象、回调顺序、错误传播、资源释放以及异步取消/背压语义。
- 接口相似不能代替行为证据；每个关键语义域都要有正常、失败和边界测试。
<!-- current-migration-contract-end -->

| Spring 语义 | Rust 对等方向 | 当前状态 |
|---|---|---|
| `TestCompiler.withSources/withResources/withProcessors` | 不可变 builder，组织临时 Cargo/源文件/资源 | `MISSING` |
| `TestCompiler.compile → DynamicJavaFileManager → DynamicClassLoader` | 编译进程、诊断捕获和隔离加载 | `MISSING` |
| `SourceFile/ResourceFile/ClassFile` | 源码、资源和产物值对象 | `MISSING` |
| `Dynamic*FileObject` | 临时文件/内存产物抽象 | `MISSING` |
| `RuntimeHintsRecorder.record` | 在作用域内记录可观测能力访问 | `MISSING` |
| `RecordedInvocationsPublisher` | 并发安全监听器注册、通知、解除 | `MISSING` |
| ASM visitor/transformer | Rust 编译产物分析或 `PLATFORM_NA` | `UNDECIDED` |
| forked classloader extension | 进程/动态库隔离或 `PLATFORM_NA` | `UNDECIDED` |

## CodeGraph 关键链

```mermaid
sequenceDiagram
    participant T as TestCompiler
    participant F as DynamicJavaFileManager
    participant C as JavaCompiler
    participant L as DynamicClassLoader
    T->>F: 合并 SourceFiles/ResourceFiles/ClassFiles
    T->>C: compile(options, processors)
    C-->>F: class/resource output
    T->>L: 装载编译结果
    L-->>T: Compiled
```

`RuntimeHintsRecorder.record` 则在执行回调前注册 listener，执行后解除并返回
`RuntimeHintsInvocations`。Rust 实现若选择 tracing/event collector，仍须保持作用域隔离和异常清理语义。

<!-- detailed-current-start -->
## vernal-core-test 语义边界

语义等价以可观察行为为准，但不能借“功能相似”消除源对象边界。

| 语义域 | Rust 目标表达 | 必须保留 | 验收证据 |
|---|---|---|---|
| 测试缓冲与流 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 反射断言 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 资源夹具 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 并发协调 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 失败诊断 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 跨平台确定性 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |

## 对象簇职责

| 目标目录 | 数量 | 代表对象 | 当前状态 |
|---|---:|---|---|
| `aot/agent` | 10 | `HintType`、`InstrumentedBridgeMethods`、`InstrumentedMethod`、`InvocationsRecorderClassTransformer`、`InvocationsRecorderClassVisitor` | MISSING:10 |
| `io/support` | 1 | `MockSpringFactoriesLoader` | MISSING:1 |
| `test/agent` | 4 | `EnabledIfRuntimeHintsAgent`、`RuntimeHintsInvocations`、`RuntimeHintsInvocationsAssert`、`RuntimeHintsRecorder` | MISSING:4 |
| `test/generate` | 2 | `CompilerFiles`、`TestGenerationContext` | MISSING:2 |
| `test/tools` | 23 | `ClassFile`、`ClassFiles`、`CompilationException`、`CompileWithForkedClassLoader`、`CompileWithForkedClassLoaderClassLoader` | MISSING:23 |

## 目标调用链

```mermaid
flowchart LR
    A["测试缓冲与流"] --> B["反射断言"]
    B --> C["资源夹具"]
    C --> D["并发协调"]
    D --> E["失败诊断"]
    E --> F["结果/错误/释放"]
    F -. 失败证据 .-> G["台账状态降级"]
```

入口、适配、执行、回调和清理都必须可观察；只验证最终返回值不足以证明顺序、取消或释放正确。

## Java 到 Rust 技术语义

| Java 机制 | Rust 映射 | 验证重点 |
|---|---|---|
| nullable | `Option<T>` | `None` 与空值不得混同 |
| checked exception | `thiserror` + `Result<T, E>` | 分类、cause 和上下文 |
| synchronized/并发容器 | `Arc<RwLock<_>>`/`DashMap` | 竞争、可见性和死锁 |
| CompletableFuture | `Future`/`JoinHandle` | 完成、失败、取消和超时 |
| Reactor Mono | `async fn -> Result<T, E>` | 单值、空结果和错误 |
| Reactor Flux | `Stream<Item=Result<T,E>>` | 背压、顺序、取消和终止 |
| ServiceLoader | `inventory` + registry | 自动注册和确定性 |
| JVM/字节码 | 有证据才可 `PLATFORM_NA` | 困难不等于不适用 |

## 语义测试矩阵

| 语义域 | 正常场景 | 失败/边界 | 观察点 |
|---|---|---|---|
| 测试缓冲与流 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 反射断言 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 资源夹具 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 并发协调 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 失败诊断 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 跨平台确定性 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |

## 语义完成清单

- [ ] 对象职责和协作边界明确。
- [ ] Javadoc 前置、后置和异常语义译为中文注释。
- [ ] 关键链路有正常、失败和边界测试。
- [ ] 异步能力覆盖完成、错误、取消、超时与背压。
- [ ] 资源型对象覆盖获取、复用和释放。
- [ ] 依赖复用有固定提交、精确符号和集成测试。
- [ ] JVM 专属能力有不可迁移证据。
- [ ] 状态严格使用验收规范定义。
<!-- detailed-current-end -->
