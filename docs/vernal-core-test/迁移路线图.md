<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-core-test 迁移路线图
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-core-test` |
| 来源基线 | `` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 40 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-core-test/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 40 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 实施顺序固定为 `MISPLACED` → `MISSING` → `STUB/PARTIAL` → `UNVERIFIED`。
- 当前优先债务：MISPLACED=0、MISSING=40、STUB=0、PARTIAL=0、UNVERIFIED=0。
- 每批必须绑定对象清单、目录调整、语义测试、审计命令和回退条件。
<!-- current-migration-contract-end -->

## P0：纳入治理

1. 明确是否创建独立 `vernal-core-test` crate。
2. 在 manifest 中登记 Spring source root、提交与 target root。
3. 生成 40 行自动对象台账；此前保持全部 `MISSING`。

## P1：对象与目录骨架

- 建立 `aot/agent`、`test/agent`、`test/generate`、`io/support`、`test/tools`。
- 仅在实现真实逻辑时创建对象文件，禁止空 trait 或 `todo!()` 充数。
- 本模块没有既存 `MISPLACED` 文件；若决定复用 `vernal-test`，也必须以精确对象和符号证据登记。

## P2：跨平台测试工具

- 先实现 source/resource/class artifact 值对象。
- 再实现编译驱动、诊断捕获、临时目录清理和隔离执行。
- 为并发编译、失败诊断、资源读取和清理补语义测试。

## P3：AOT/Agent 裁决

- 对 16 个 AOT/Agent 对象逐项决定 `IMPLEMENTED`、`DEPENDENCY_REUSED` 或 `PLATFORM_NA`。
- 不得把整包一次性标为 JVM 专属；监听器、调用记录等语义可跨平台实现。

## 完成门禁

40 个对象全部进入三种严格完成状态，并通过 crate 测试、Clippy 和自动审计后，才可标记完成。

<!-- detailed-current-start -->
## vernal-core-test 当前基线

| 状态 | 数量 | 计入完成 |
|---|---:|---|
| `DEPENDENCY_REUSED` | 0 | 是 |
| `IMPLEMENTED` | 0 | 是 |
| `MISPLACED` | 0 | 否 |
| `MISSING` | 40 | 否 |
| `PARTIAL` | 0 | 否 |
| `PLATFORM_NA` | 0 | 是 |
| `STUB` | 0 | 否 |
| `UNVERIFIED` | 0 | 否 |

对象总数 **40**；只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成。

## 阶段与退出条件

| 阶段 | 工作 | 退出条件 | 失败处理 |
|---|---|---|---|
| P0 台账冻结 | 固定提交、对象和路径 | 数量可复现且无重复 | 回到源码扫描 |
| P1 结构校正 | 优先处理 `MISPLACED` | 无目录错位 | 回退单对象移动 |
| P2 对象补齐 | 建立一对象一文件真实实现 | 类型和来源注释齐全 | 保持未完成 |
| P3 语义实现 | 生命周期和错误模型 | 单对象语义测试通过 | 降级 `PARTIAL` |
| P4 集成 | 跨对象、依赖和并发链 | 正常与失败集成测试 | 隔离适配层 |
| P5 固化 | 审计、测试、Clippy、文档 | 证据绑定提交和日期 | 漂移即降级 |

## 目录批次

| 批次 | 目标目录 | 数量 | 当前状态 | 交付证据 |
|---|---|---:|---|---|
| D01 | `aot/agent` | 10 | MISSING:10 | 模块声明、对象测试、审计报告 |
| D02 | `io/support` | 1 | MISSING:1 | 模块声明、对象测试、审计报告 |
| D03 | `test/agent` | 4 | MISSING:4 | 模块声明、对象测试、审计报告 |
| D04 | `test/generate` | 2 | MISSING:2 | 模块声明、对象测试、审计报告 |
| D05 | `test/tools` | 23 | MISSING:23 | 模块声明、对象测试、审计报告 |

## 对象批次

| 批次 | 对象范围 | 当前状态 | 完成证据 |
|---|---|---|---|
| O01 | `HintType`、`InstrumentedBridgeMethods`、`InstrumentedMethod`、`InvocationsRecorderClassTransformer`、`InvocationsRecorderClassVisitor`、`MethodReference`、`RecordedInvocation`、`RecordedInvocationsListener`、`RecordedInvocationsPublisher`、`RuntimeHintsAgent`、`MockSpringFactoriesLoader`、`EnabledIfRuntimeHintsAgent`、`RuntimeHintsInvocations`、`RuntimeHintsInvocationsAssert`、`RuntimeHintsRecorder`、`CompilerFiles`、`TestGenerationContext`、`ClassFile`、`ClassFiles`、`CompilationException` | MISSING | 路径、注释、单测和集成证据 |
| O02 | `CompileWithForkedClassLoader`、`CompileWithForkedClassLoaderClassLoader`、`CompileWithForkedClassLoaderExtension`、`Compiled`、`DynamicClassFileObject`、`DynamicClassLoader`、`DynamicFile`、`DynamicFileAssert`、`DynamicFiles`、`DynamicJavaFileManager`、`DynamicJavaFileObject`、`DynamicResourceFileObject`、`ResourceFile`、`ResourceFileAssert`、`ResourceFiles`、`SourceFile`、`SourceFileAssert`、`SourceFiles`、`TestCompiler`、`WritableContent` | MISSING | 路径、注释、单测和集成证据 |

## 语义能力顺序

1. **测试缓冲与流**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
2. **反射断言**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
3. **资源夹具**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
4. **并发协调**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
5. **失败诊断**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
6. **跨平台确定性**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。

## 每批强制门禁

- [ ] `python3 scripts/audit_migration_docs.py --module vernal-core-test --check` 通过。
- [ ] 目录和 snake_case 文件名与对象表一致。
- [ ] 每个文件只有一个源对象且包含真实逻辑。
- [ ] 无 todo、unimplemented、占位字段或空业务分支。
- [ ] 对象和公开方法具有中文来源注释。
- [ ] 正常、失败、边界、并发/取消和释放测试可定位。
- [ ] 生产代码无 wildcard import。
- [ ] `lib.rs`/`mod.rs` 只声明和重导出。
- [ ] 依赖复用精确到提交、符号和集成测试。
- [ ] 统计从当前工作树生成，不手写完成率。

## 最终验收命令

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --module vernal-core-test --check
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --all --check
cargo test -p vernal-core-test
cargo clippy -p vernal-core-test --all-targets -- -D warnings
```

> 若目标 crate 不存在，Cargo 命令必须记录为“目标 crate 不存在”；不得伪造通过。
<!-- detailed-current-end -->
