<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-orm 迁移路线图
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-orm` |
| 来源基线 | `org.springframework.orm` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 59 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-orm/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 59 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 实施顺序固定为 `MISPLACED` → `MISSING` → `STUB/PARTIAL` → `UNVERIFIED`。
- 当前优先债务：MISPLACED=0、MISSING=59、STUB=0、PARTIAL=0、UNVERIFIED=0。
- 每批必须绑定对象清单、目录调整、语义测试、审计命令和回退条件。
<!-- current-migration-contract-end -->

1. 创建 crate 与 `jpa/`、`jpa/support/`、`jpa/hibernate/`、`jpa/vendor/` 目录。
2. 按[59 行对象表](对象级对照表.md)先实现异常翻译、dialect、holder 与 factory utils。
3. 实现事务管理器的 begin/commit/rollback/cleanup 和 datasource 联动。
4. 实现 shared entity manager、factory bean、persistence unit 与 open-in-view 生命周期。
5. 逐对象审查 Hibernate/JPA/JVM 专属能力；以真实 provider 集成测试验证复用。

所有对象进入严格三态前不得宣称 ORM 模块完成。

<!-- detailed-current-start -->
## vernal-orm 当前基线

| 状态 | 数量 | 计入完成 |
|---|---:|---|
| `DEPENDENCY_REUSED` | 0 | 是 |
| `IMPLEMENTED` | 0 | 是 |
| `MISPLACED` | 0 | 否 |
| `MISSING` | 59 | 否 |
| `PARTIAL` | 0 | 否 |
| `PLATFORM_NA` | 0 | 是 |
| `STUB` | 0 | 否 |
| `UNVERIFIED` | 0 | 否 |

对象总数 **59**；只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成。

## 阶段与退出条件

| 阶段 | 工作 | 退出条件 | 失败处理 |
|---|---|---|---|
| P0 台账冻结 | 固定提交、对象和路径 | 数量可复现且无重复 | 回到源码扫描 |
| P1 结构校正 | 优先处理 `MISPLACED` | 无目录错位 | 回退单对象移动 |
| P2 对象补齐 | 建立一对象一文件真实实现 | 类型和来源注释齐全 | 保持未完成 |
| P3 语义实现 | 生命周期和错误模型 | 单对象语义测试通过 | 降级 `PARTIAL` |
| P4 集成 | 跨对象、依赖和并发链 | 正常与失败集成测试 | 隔离适配层 |
| P5 固化 | 审计、测试、Clippy、文档 | 证据绑定提交和日期 | 漂移即降级 |

## 目录批次

| 批次 | 目标目录 | 数量 | 当前状态 | 交付证据 |
|---|---|---:|---|---|
| D01 | `crate 根目录` | 2 | MISSING:2 | 模块声明、对象测试、审计报告 |
| D02 | `jpa` | 18 | MISSING:18 | 模块声明、对象测试、审计报告 |
| D03 | `jpa/hibernate` | 12 | MISSING:12 | 模块声明、对象测试、审计报告 |
| D04 | `jpa/persistenceunit` | 14 | MISSING:14 | 模块声明、对象测试、审计报告 |
| D05 | `jpa/support` | 6 | MISSING:6 | 模块声明、对象测试、审计报告 |
| D06 | `jpa/vendor` | 7 | MISSING:7 | 模块声明、对象测试、审计报告 |

## 对象批次

| 批次 | 对象范围 | 当前状态 | 完成证据 |
|---|---|---|---|
| O01 | `AbstractEntityManagerFactoryBean`、`DefaultJpaDialect`、`EntityManagerFactoryAccessor`、`EntityManagerFactoryInfo`、`EntityManagerFactoryUtils`、`EntityManagerHolder`、`EntityManagerProxy`、`EntityManagerRuntimeHints`、`ExtendedEntityManagerCreator`、`ConfigurableJtaPlatform`、`HibernateExceptionTranslator`、`HibernateTransactionManager`、`LocalSessionFactoryBean`、`LocalSessionFactoryBuilder`、`SessionHolder`、`SharedSessionCreator`、`SpringBeanContainer`、`SpringFlushSynchronization`、`SpringJtaSessionContext`、`SpringSessionContext` | MISSING | 路径、注释、单测和集成证据 |
| O02 | `SpringSessionSynchronization`、`JpaDialect`、`JpaObjectRetrievalFailureException`、`JpaOptimisticLockingFailureException`、`JpaSystemException`、`JpaTransactionManager`、`JpaVendorAdapter`、`LocalContainerEntityManagerFactoryBean`、`LocalEntityManagerFactoryBean`、`ClassFileTransformerAdapter`、`DefaultPersistenceUnitManager`、`ManagedClassNameFilter`、`MutablePersistenceUnitInfo`、`PersistenceManagedTypes`、`PersistenceManagedTypesBeanRegistrationAotProcessor`、`PersistenceManagedTypesScanner`、`PersistenceUnitManager`、`PersistenceUnitPostProcessor`、`PersistenceUnitReader`、`SimplePersistenceManagedTypes` | MISSING | 路径、注释、单测和集成证据 |
| O03 | `SmartPersistenceUnitInfo`、`SpringPersistenceUnitInfo`、`SpringPersistenceUnitInfoRuntimeHints`、`SharedEntityManagerCreator`、`AsyncRequestInterceptor`、`InjectionCodeGenerator`、`OpenEntityManagerInViewFilter`、`OpenEntityManagerInViewInterceptor`、`PersistenceAnnotationBeanPostProcessor`、`SharedEntityManagerBean`、`AbstractJpaVendorAdapter`、`Database`、`EclipseLinkJpaDialect`、`EclipseLinkJpaVendorAdapter`、`HibernateJpaDialect`、`HibernateJpaVendorAdapter`、`SpringHibernateJpaPersistenceProvider`、`ObjectOptimisticLockingFailureException`、`ObjectRetrievalFailureException` | MISSING | 路径、注释、单测和集成证据 |

## 语义能力顺序

1. **会话生命周期**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
2. **异常转换**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
3. **事务桥接**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
4. **实体回调**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
5. **查询映射**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
6. **依赖隔离**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。

## 每批强制门禁

- [ ] `python3 scripts/audit_migration_docs.py --module vernal-orm --check` 通过。
- [ ] 目录和 snake_case 文件名与对象表一致。
- [ ] 每个文件只有一个源对象且包含真实逻辑。
- [ ] 无 todo、unimplemented、占位字段或空业务分支。
- [ ] 对象和公开方法具有中文来源注释。
- [ ] 正常、失败、边界、并发/取消和释放测试可定位。
- [ ] 生产代码无 wildcard import。
- [ ] `lib.rs`/`mod.rs` 只声明和重导出。
- [ ] 依赖复用精确到提交、符号和集成测试。
- [ ] 统计从当前工作树生成，不手写完成率。

## 最终验收命令

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --module vernal-orm --check
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --all --check
cargo test -p vernal-orm
cargo clippy -p vernal-orm --all-targets -- -D warnings
```

> 若目标 crate 不存在，Cargo 命令必须记录为“目标 crate 不存在”；不得伪造通过。
<!-- detailed-current-end -->
