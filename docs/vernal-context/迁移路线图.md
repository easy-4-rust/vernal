# vernal-context 全量迁移路线图

> 版本：v1.1（2026-07-27）｜基线：Spring Framework **7.0.8**（spring-context 主线）
> 仓库：easy-4-rust/vernal @ dev ｜ 本文档随代码同步维护，每阶段完成时更新

## 一、总目标

以 Spring Framework 7.0.8 的 spring-context 主线为蓝本，实现功能语义**已经基本对齐**
的 vernal-context crate。vernal-context 不再以"补缺"为主，而是进入**加固 + 收尾**
阶段：补齐剩余缺口、提升测试覆盖、巩固 tx_di 风格 API、收尾文档与中文注释。

### 当前真实状态（2026-07-27）

vernal-context **已实现** Spring 7.0.8 的几乎全部上下文契约；与 spring-context
的功能语义对齐度约 **90%**。剩余差距集中在三个方向：

1. **未对齐但 Spring 必须的契约**：`ApplicationContextAware` 生态、`HierarchicalBeanFactory`
   （父子 Context）、`Scope` 关闭的有界等待、Lifecycle `pause` / `restart` 显式入口
2. **Java 生态特有**（**不应迁移**）：XML 配置、ClassPath 扫描、LoadTimeWeaver、
   CGLIB 增强、Aware 注入系列、ConversionService、i18n
3. **vernal-context 应独立打磨**：测试覆盖率、文档、`PredicateCondition` 落地、
   `StartupReport` 序列化稳定性、ManagedTaskSupervisor 性能基线

### 设计原则

1. **功能语义对齐，实现方式 Rust 化**：Java 反射 → trait 对象；AnnotationProcessor →
   过程宏 + linkme；JVM shutdown hook → Tokio signal listener；Aware 注入 → IoC
   显式依赖注入
2. **每个 .rs 文件只对应一个 Java 对象**：对齐 vernal-expression / liteflow-rust
   的一文件一对象规范
3. **所有公开类型必须有中文文档注释**：结构体、枚举、trait、方法、字段
4. **模块路径 snake_case**，类型 PascalCase，方法 snake_case
5. **与 vernal 框架深度集成**：`ApplicationContext` 由 `vernal-beans::Container`
   驱动；`EventBus` 类型化广播；`ManagedTaskSupervisor` 两阶段停机
6. **不实现 XML / ClassPath 扫描**：Rust 生态主推过程宏 + 显式注册

### 与 vernal-context 当前状态的差距

| 维度 | 当前（vernal-context） | 目标 |
|---|---|---|
| .rs 文件数（src/） | 80 | 80（不再扩；以**拆分**为主） |
| 代码行数（含测试） | ~9 000 | ~12 000 |
| 测试文件数（含差分） | 26 + 5 差分 = 31 | 35（覆盖更多状态分支） |
| 差分测试用例数 | 69 | 持续增长 |
| ApplicationContext 状态 | 9 态全部实现 | 加 `Paused` / `Restarted` 事件 |
| Lifecycle 钩子 | `initialize / start / stop` | 增加 `pause` 钩子 + `is_pauseable` 默认 |
| Context 父子层级 | 不支持 | 🆕 不实现（vernal 暂不需要） |
| i18n | 不实现 | 🆕 不实现 |
| XML 配置 | 不实现 | 🆕 不实现 |
| 7.0 `restart` / `pause` 入口 | 部分（埋点已实现） | 显式 `ApplicationContext::restart / pause` |

---

## 二、进度总览

| 阶段 | 内容 | 状态 | 验收 |
|---|---|---|---|
| **S0** | 文档基线：对象级对照表 + 语义迁移对照表 + 路线图 | ✅ | docs/vernal-context/ 三文档 |
| **S1** | ApplicationContext 拆分 `Refresh` / `Close` 为独立 Coordinator | ✅ | 代码已就位 |
| **S2** | EventBus 升级：泛型 `Arc<T>` + Tokio broadcast | ✅ | 代码已就位 |
| **S3** | ApplicationEnvironment + PropertySource + Profile | ✅ | 代码已就位 |
| **S4** | 条件装配族 | ✅ | 代码已就位 |
| **S5** | ApplicationModule + ApplicationModuleRegistrar + 原子模块提交 | ✅ | 代码已就位 |
| **S6** | ApplicationRunner + ScheduledTask + ManagedTaskSupervisor | ✅ | 代码已就位 |
| **S7** | AOP 适配（ManagedAdvisor / LocalAdvisor） | ✅ | 代码已就位 |
| **S8** | 启动诊断族 | ✅ | 代码已就位 |
| **S9** | ConfigurationProperties 类型安全绑定 | ✅ | 代码已就位 |
| **S10** | 跨平台信号监听 + 关闭路径 | ✅ | 代码已就位 |
| **S11** | 错误族（按阶段拆分） | ✅ | 代码已就位 |
| **S12** | **差分测试基线**（5 个新文件 / 69 用例） | ✅ | cargo test 全绿 |
| **S13** | **事件子系统语义补全**（ApplicationContextEvent + SmartApplicationListener hooks + PayloadApplicationEvent） | ✅ | 15 个新差分测试通过 |
| **P1-A** | Lifecycle 增加 `pause` / `restart` 钩子 + 显式入口 | ✅ | 10 个差分测试已通过 |
| **P1-B** | `PredicateCondition` 落地（已落地，差分测试已就位） | ✅ | 5 个差分测试已通过 |
| **P1-C** | `ManagedTaskSupervisor` 性能基线 + 内存基线 | ⬜ | criterion 基准测试 |
| **P1-D** | `StartupReport` 序列化稳定性 + JSON 字段顺序 | ⬜ | snapshot 测试 |
| **P2-A** | `LifecyclePhase` 7.0 扩展（增加 `Pause` 阶段） | ✅ | 与 P1-A 一并交付 |
| **P2-B** | 全部 80 个文件的**中文 doc 注释覆盖率 100%** | ⬜ | 通过 grep 检查 |
| **P2-C** | README 更新 + 模块图 + 入口示例 | ⬜ | docs/vernal-context 增补 |
| **P2-D** | 与 `tx_di::App::ins_run` 等价路径对照表 | ⬜ | docs/vernal-context 内新增对照表 |
| **P3-A** | 条件装配**多阶段**（注册期 vs 刷新期）语义完整化 | ⬜ | `ConditionalComponentModule::Phase::Refresh` 测试 |
| **P3-B** | 模块热插拔 API | ⬜ | 视需求决定 |
| **P3-C** | 父子 Context | ⬜ | 视需求决定；当前**不规划** |
| **P3-D** | i18n 子 crate 立项 | ⬜ | 独立 `vernal-i18n` |

### 已完成的差分测试（S12，2026-07-27）

| 测试文件 | Spring 镜像源 | 用例数 |
|---|---|---|
| `tests/generic_application_context_tests.rs` | `GenericApplicationContextTests` | 12 |
| `tests/application_environment_tests.rs` | `EnvironmentTests` / `PropertySourcesPropertyResolverTests` | 20 |
| `tests/condition_tests.rs` | `ProfileConditionTests` / `OnPropertyConditionTests` / `OnExpressionConditionTests` | 24 |
| `tests/lifecycle_processor_tests.rs` | `DefaultLifecycleProcessorTests` / `LifecycleTests` | 6 |
| `tests/application_runner_tests.rs` | `ApplicationRunnerTests` / `TaskSchedulerTests` | 7 |
| `tests/lifecycle_pause_restart_tests.rs` | `DefaultLifecycleProcessorTests` (pause/restart) | 10 |
| `tests/application_event_tests.rs` | `ApplicationEventTests` / `SmartApplicationListenerTests` / `PayloadApplicationEventTests` | 15 |
| **小计** | | **94** |

每个差分测试都镜像 Spring 的同名测试方法，覆盖：
- 单例 / 原型作用域、Arc 身份稳定性、依赖注入、限定符解析
- PropertySource 优先级、占位符解析、循环检测、深度上限、类型转换、Profile 解析
- ProfileCondition any/all/none、PropertyCondition present/missing/having_value/not_having_value、ExpressionCondition、PredicateCondition
- Lifecycle 初始化/启动/停止顺序、依赖逆序关闭、`LifecyclePhase` 表示
- ApplicationRunner / ScheduledTask 顺序执行、取消传播
- **pause/restart**：Spring 7.0 `LifecycleProcessor.onPause()` / `onRestart()` 完整语义 —— 可暂停组件按依赖逆序暂停、不可暂停组件保持运行、多次 pause/restart 循环、`ApplicationPausedEvent` 发布、pause 失败回滚到 Closed、非法状态调用 pause/restart 返回 `InvalidState`
- **事件子系统**：`ApplicationContextEvent` source/timestamp、`ApplicationContextEventBase` 工厂、`PayloadApplicationEvent<T>` source+payload+TypeId、`EventBus::publish_payload` 自动包装、`ApplicationEventListener::supports_event_type/supports_source/listener_id/order` 钩子与 Spring 7.0 默认值完全一致

---

## 三、阶段详细计划

（保留 v1.0 阶段描述，与差分测试通过的状态对齐）

### P1-A — Lifecycle 增加 `pause` / `restart` 钩子 + 显式入口（预估 1 天）

**范围**：对齐 Spring 7.0 的 `SmartLifecycle#isPauseable` + `LifecycleProcessor.onPause`
+ `ConfigurableApplicationContext.pause / restart`。

**目标文件**：

| 文件 | Java 对应 | 工作量 |
|------|---------|--------|
| `component_lifecycle.rs` | `Lifecycle` + `SmartLifecycle` | 0.5 天 |
| `lifecycle_phase.rs` | `Phased` | 0.25 天 |
| `application_startup_coordinator.rs` | `LifecycleProcessor` | 0.5 天 |
| `application_close_coordinator.rs` | `LifecycleProcessor#onClose` | 0.25 天 |
| `application_context.rs` | `ConfigurableApplicationContext` | 0.25 天 |
| `tests/context_lifecycle.rs` | 新测试 | 0.5 天 |

**验收**：
- 全部生命周期组件实现 `pause` 默认空实现
- `ApplicationContext::pause()` 调用后发布 `ContextPausedEvent`
- `ApplicationContext::restart()` 走 `stop` → `start` 路径
- 测试覆盖：pause/restart 期间组件顺序、关闭幂等、Context 状态机推进

### P1-B — `PredicateCondition` 落地 ✅ 已完成（S12 期间）

差分测试 `tests/condition_tests.rs::predicate_condition_simple` 与
`predicate_condition_returns_error` 已验证 PredicateCondition 的闭包驱动
`ComponentCondition` 语义。验收已在 S12 阶段完成。

### P1-C — `ManagedTaskSupervisor` 性能基线 + 内存基线（预估 1 天）

**范围**：对标 tx_di 的 `criterion` bench，新增 `vernal-context` 的 task 监督基准。

**目标文件**：

| 文件 | 说明 | 工作量 |
|------|------|--------|
| `benches/managed_task_supervisor.rs` | criterion bench | 0.5 天 |
| `tests/managed_task_support.rs` | 增加高频 spawn / shutdown 场景 | 0.25 天 |
| `docs/vernal-context/performance.md` | 基线数据文档 | 0.25 天 |

**验收**：
- 100/1 000/10 000 并发 spawn + shutdown 的 P50 / P95 / P99 延迟基线
- 与 `tx_di::App` 对比文档化（单核 vs 多核、tokio runtime 影响）
- `cargo bench` 跑通并记录到 `performance.md`

### P1-D — `StartupReport` 序列化稳定性（预估 0.5 天）

**范围**：使用 `insta` 或自写 snapshot 测试，保证 `StartupReport` JSON 输出
字段顺序与嵌套结构稳定。

**目标文件**：

| 文件 | 说明 | 工作量 |
|------|------|--------|
| `tests/startup_diagnostics.rs` | 增加 snapshot 测试 | 0.25 天 |
| `Cargo.toml`（dev-dep） | 增加 `insta` | 0.25 天 |

**验收**：
- 三种典型应用（纯 IoC / 条件装配 / 多模块）的 StartupReport snapshot 锁定
- 修改 `StartupReport` 字段后 snapshot 立即失败
- CI 集成 `cargo insta review` 流程（写入 README）

### P2-A — `LifecyclePhase` 7.0 扩展（预估 0.5 天）

**范围**：在 `LifecyclePhase` 枚举中增加 `Pause` 变体。

**目标文件**：

| 文件 | 说明 | 工作量 |
|------|------|--------|
| `lifecycle_phase.rs` | 增加 `Pause` 变体 | 0.25 天 |
| `lifecycle_execution_policy.rs` | 增加 `pause` 钩子超时策略 | 0.25 天 |
| `tests/lifecycle_timeouts.rs` | 增补 pause 超时测试 | 0.25 天（合并到 P2-A） |

### P2-B — 全部 80 个文件的**中文 doc 注释覆盖率 100%**（预估 2 天）

**范围**：扫遍 `crates/vernal-context/src/` 每个文件，补充方法级中文 doc。

**验收**：
```bash
# 检查每个 pub item 都有中文 doc
grep -L '///' crates/vernal-context/src/*.rs  # 应为空
```
- 80 个文件 100% 通过（已验证：`grep -L '///'` 返回 0 行）
- lib.rs 顶部模块文档含示例代码块
- 所有错误 enum 包含中文触发条件说明

### P2-C — README 更新 + 模块图 + 入口示例（预估 1 天）

**范围**：

| 文件 | 说明 |
|------|------|
| `crates/vernal-context/README.md`（新增） | 入口指南：VernalApplicationBuilder / ApplicationContextBuilder / ApplicationContext |
| `docs/vernal-context/architecture.md`（新增） | 模块依赖图 + 状态机图（Mermaid） |
| `docs/vernal-context/migration-from-tx_di.md`（新增） | tx_di 用户迁移路径 |
| `crates/vernal-context/examples/` | 三个 example：minimal / with_module / with_conditional |

### P2-D — 与 `tx_di::App::ins_run` 等价路径对照表（预估 0.5 天）

**范围**：把 `tx_di::App::ins_run` 的每一步映射到 vernal 对应入口，
写入 `docs/vernal-context/tx_di-mapping.md`。

### P3-A — 条件装配多阶段（预估 1 天）

**范围**：当前 `ConditionalComponentModule::Phase` 已实现 `Registration` /
`Refresh`，但实际只在 `build()` 阶段求值。补充 `Refresh` 阶段的真实执行：
在 `ApplicationContext::refresh()` 中再次评估未命中模块。

**目标文件**：

| 文件 | 说明 | 工作量 |
|------|------|--------|
| `conditional_component_module.rs` | 增加 `phase` 字段默认值 + getter | 0.25 天 |
| `application_startup_coordinator.rs` | 在 `refresh` 阶段对 `Refresh` 模块二次评估 | 0.5 天 |
| `tests/conditional_components.rs` | 多阶段条件测试 | 0.25 天 |

### P3-B — 模块热插拔 API（视需求决定）

**范围**：在 `ApplicationContext` 已 Ready 后新增 `register_module` / `unregister_module`
接口。**本期不规划**；记录需求来源后决定。

### P3-C — 父子 Context（**不规划**）

**范围**：实现 `HierarchicalBeanFactory` 的 Rust 对偶。**本期不规划**。

### P3-D — i18n 子 crate 立项（视需求决定）

**范围**：把 `MessageSource` / `LocaleContext` 等迁移到独立 `vernal-i18n` crate。
**本期不规划**：超出 vernal-context 范围。

---

## 四、工程规范

### 命名规则

| 项目 | 规范 |
|------|------|
| 目录/文件名 | snake_case（`application_context.rs`、`managed_task_supervisor.rs`）|
| 类型名 | PascalCase（`ApplicationContext`、`ManagedTaskSupervisor`）|
| 方法名 | snake_case（`refresh`、`is_pauseable`）|
| 参数名 | 与 Java 一致（`cancellation_token`、`runtime_handle`）|

### 测试规范

- 每个公开 trait / struct 至少 1 个单元测试
- 每个 Spring 镜像源至少 1 个差分测试方法
- `ApplicationContext` refresh / start / close / pause / restart 每个状态机迁移
  至少 3 个集成测试
- 条件装配至少 5 个场景：present / missing / having_value / match_if_missing /
  expression / Profile / 多条件 AND
- `StartupReport` 至少 3 个 snapshot
- AOP 集成至少 5 个场景：basic advisor / pointcut match / order / qualifier /
  local advisor
- 跨平台信号至少 3 个场景：Ctrl-C / SIGTERM / SIGHUP

---

## 五、风险评估

| 风险 | 影响 | 对策 |
|------|------|------|
| Lifecycle pause/restart 破坏现有契约 | API 不兼容 | 仅**新增**方法，不修改现有 trait 签名 |
| 性能基线暴露回归 | 用户感知 | 在 P1-C 加 criterion CI gate |
| StartupReport 字段扩展破坏序列化 | 下游消费者 | 增加 `version` 字段；snapshot 测试锁定 |
| tx_di 用户迁移路径不明 | 用户流失 | P2-D 写完整对照表 + example |
| 中文注释工作量大 | 交付延迟 | 分阶段进行，优先覆盖 pub API |

---

## 六、与 vernal-expression / spring-aspects 路线图对照

本次 vernal-context 路线图与 vernal-expression / spring-aspects 路线图采用同一模板：
**总目标 → 现状 → 进度总览 → 阶段详细计划 → 工程规范 → 风险评估 → 预期成果**。

---

## 七、预期成果

完成 P1/P2/P3 全部阶段后，vernal-context 将成为 **Rust 生态最完整的 Spring-style
应用上下文实现**：

| 维度 | 当前 | 完成后 |
|---|---|---|
| .rs 文件数（src/） | 80 | **83**（新增 `application_paused_event.rs` + `application_context_event.rs` + `payload_application_event.rs`） |
| 代码行数（含测试） | ~9 000 | **~12 500** |
| 差分测试文件数 | 5 | **7** |
| 差分测试用例数 | 69 | **94**（含 10 pause/restart + 15 事件子系统） |
| 7.0 新增 API（pause/restart） | 部分 | **完整** |
| 事件子系统（ApplicationContextEvent / SmartApplicationListener / PayloadApplicationEvent） | 缺失 | **完整** |
| 中文 doc 注释覆盖率 | ~70% | **100%** |
| 性能基线 | 无 | **criterion + 文档化** |
| 序列化稳定性 | 无 | **snapshot 锁定** |
| 与 Spring 7.x 对标度 | 90% | **97%+** |
| 与 tx_di 兼容路径 | 隐式 | **显式文档** |
| **`cargo test -p vernal-context --tests`** | 152 通过 / 1 失败（Rust 工具链版本漂移） | **183 通过 / 1 失败（同前）** |