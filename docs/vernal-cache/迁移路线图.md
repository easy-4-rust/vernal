<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# vernal-cache 迁移路线图
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-cache` |
| 来源基线 | `org.springframework.cache` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 63 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-cache/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 0 |
| `MISSING` | 62 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 1 |

### 本文档执行责任

- 实施顺序固定为 `MISPLACED` → `MISSING` → `STUB/PARTIAL` → `UNVERIFIED`。
- 当前优先债务：MISPLACED=0、MISSING=62、STUB=0、PARTIAL=0、UNVERIFIED=1。
- 每批必须绑定对象清单、目录调整、语义测试、审计命令和回退条件。
<!-- current-migration-contract-end -->

## P0：审计和错位

先生成 63 行对象报告，处理所有 `MISPLACED`，尤其把泛化 `manager.rs` 拆为同名对象文件。

## P1：核心合同

迁移根包 `Cache`、`CacheManager`、异常和 value wrapper；补齐空值、原子 put、失效与并发测试。

## P2：操作与拦截

迁移 `annotation/`、`interceptor/`、`support/`，覆盖 key、condition/unless、同步加载与提前/延后淘汰。

## P3：复用与集成

逐对象登记 `vernal-context-support` 或外部 cache crate 的精确复用符号；加入缓存切面、事务同步和异步结果的集成测试。

完成时不得存在 `MISPLACED`、`MISSING`、`STUB`、`PARTIAL` 或 `UNVERIFIED`。

<!-- detailed-current-start -->
## vernal-cache 当前基线

| 状态 | 数量 | 计入完成 |
|---|---:|---|
| `DEPENDENCY_REUSED` | 0 | 是 |
| `IMPLEMENTED` | 0 | 是 |
| `MISPLACED` | 0 | 否 |
| `MISSING` | 62 | 否 |
| `PARTIAL` | 0 | 否 |
| `PLATFORM_NA` | 0 | 是 |
| `STUB` | 0 | 否 |
| `UNVERIFIED` | 1 | 否 |

对象总数 **63**；只有 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成。

## 阶段与退出条件

| 阶段 | 工作 | 退出条件 | 失败处理 |
|---|---|---|---|
| P0 台账冻结 | 固定提交、对象和路径 | 数量可复现且无重复 | 回到源码扫描 |
| P1 结构校正 | 优先处理 `MISPLACED` | 无目录错位 | 回退单对象移动 |
| P2 对象补齐 | 建立一对象一文件真实实现 | 类型和来源注释齐全 | 保持未完成 |
| P3 语义实现 | 生命周期和错误模型 | 单对象语义测试通过 | 降级 `PARTIAL` |
| P4 集成 | 跨对象、依赖和并发链 | 正常与失败集成测试 | 隔离适配层 |
| P5 固化 | 审计、测试、Clippy、文档 | 证据绑定提交和日期 | 漂移即降级 |

## 目录批次

| 批次 | 目标目录 | 数量 | 当前状态 | 交付证据 |
|---|---|---:|---|---|
| D01 | `annotation` | 14 | MISSING:14 | 模块声明、对象测试、审计报告 |
| D02 | `concurrent` | 3 | MISSING:3 | 模块声明、对象测试、审计报告 |
| D03 | `config` | 4 | MISSING:4 | 模块声明、对象测试、审计报告 |
| D04 | `crate 根目录` | 2 | MISSING:1、UNVERIFIED:1 | 模块声明、对象测试、审计报告 |
| D05 | `interceptor` | 32 | MISSING:32 | 模块声明、对象测试、审计报告 |
| D06 | `support` | 8 | MISSING:8 | 模块声明、对象测试、审计报告 |

## 对象批次

| 批次 | 对象范围 | 当前状态 | 完成证据 |
|---|---|---|---|
| O01 | `AbstractCachingConfiguration`、`AnnotationCacheOperationSource`、`CacheAnnotationParser`、`CacheConfig`、`CacheEvict`、`CachePut`、`Cacheable`、`Caching`、`CachingConfigurationSelector`、`CachingConfigurer`、`CachingConfigurerSupport`、`EnableCaching`、`ProxyCachingConfiguration`、`SpringCacheAnnotationParser`、`CacheManager`、`ConcurrentMapCache`、`ConcurrentMapCacheFactoryBean`、`ConcurrentMapCacheManager`、`AnnotationDrivenCacheBeanDefinitionParser`、`CacheAdviceParser` | MISSING | 路径、注释、单测和集成证据 |
| O02 | `CacheManagementConfigUtils`、`CacheNamespaceHandler`、`AbstractCacheInvoker`、`AbstractCacheResolver`、`AbstractFallbackCacheOperationSource`、`BasicOperation`、`BeanFactoryCacheOperationSourceAdvisor`、`CacheAspectSupport`、`CacheErrorHandler`、`CacheEvaluationContext`、`CacheEvaluationContextFactory`、`CacheEvictOperation`、`CacheExpressionRootObject`、`CacheInterceptor`、`CacheOperation`、`CacheOperationExpressionEvaluator`、`CacheOperationInvocationContext`、`CacheOperationInvoker`、`CacheOperationSource`、`CacheOperationSourcePointcut` | MISSING | 路径、注释、单测和集成证据 |
| O03 | `CacheProxyFactoryBean`、`CachePutOperation`、`CacheResolver`、`CacheableOperation`、`CompositeCacheOperationSource`、`KeyGenerator`、`LoggingCacheErrorHandler`、`NameMatchCacheOperationSource`、`NamedCacheResolver`、`SimpleCacheErrorHandler`、`SimpleCacheResolver`、`SimpleKey`、`SimpleKeyGenerator`、`VariableNotAvailableException`、`AbstractCacheManager`、`AbstractValueAdaptingCache`、`CompositeCacheManager`、`NoOpCache`、`NoOpCacheManager`、`NullValue` | MISSING | 路径、注释、单测和集成证据 |
| O04 | `SimpleCacheManager`、`SimpleValueWrapper`、`Cache` | MISSING、UNVERIFIED | 路径、注释、单测和集成证据 |

## 语义能力顺序

1. **缓存与管理器**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
2. **读写淘汰清空**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
3. **键与条件**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
4. **并发加载**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
5. **注解拦截**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。
6. **依赖复用边界**：冻结接口和错误类型，完成正常链路，再补失败、取消、并发与释放测试。

## 每批强制门禁

- [ ] `python3 scripts/audit_migration_docs.py --module vernal-cache --check` 通过。
- [ ] 目录和 snake_case 文件名与对象表一致。
- [ ] 每个文件只有一个源对象且包含真实逻辑。
- [ ] 无 todo、unimplemented、占位字段或空业务分支。
- [ ] 对象和公开方法具有中文来源注释。
- [ ] 正常、失败、边界、并发/取消和释放测试可定位。
- [ ] 生产代码无 wildcard import。
- [ ] `lib.rs`/`mod.rs` 只声明和重导出。
- [ ] 依赖复用精确到提交、符号和集成测试。
- [ ] 统计从当前工作树生成，不手写完成率。

## 最终验收命令

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s scripts/tests
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --module vernal-cache --check
PYTHONDONTWRITEBYTECODE=1 python3 scripts/audit_migration_docs.py --all --check
cargo test -p vernal-cache
cargo clippy -p vernal-cache --all-targets -- -D warnings
```

> 若目标 crate 不存在，Cargo 命令必须记录为“目标 crate 不存在”；不得伪造通过。
<!-- detailed-current-end -->
