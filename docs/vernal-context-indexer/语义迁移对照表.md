<!-- migration-doc: authority=authoritative canonical=../迁移验收规范.md -->
# spring-context-indexer → vernal-context-indexer 语义迁移对照表
> 迁移文档治理：本文级别为 **authoritative**。正文中的历史统计或完成标记不得单独作为验收结论；以 [../迁移验收规范.md](../迁移验收规范.md) 和自动审计报告为准。

<!-- current-migration-contract-start -->
## 当前迁移规范执行口径

| 规范项 | 本模块当前要求 |
|---|---|
| 模块 | `vernal-context-indexer` |
| 来源基线 | `org.springframework.context.index` @ `9e8cea3ef8ae02efb7956b071cd7bbef7c22cb82` |
| 当前对象边界 | 12 个对象；不把 `package-info.java`、合并对象或模糊依赖豁免计入完成 |
| 目标根 | `crates/vernal-context-indexer/src` |
| 目录和文件 | 去掉组织及模块根包，保留末两层；目录/文件 snake_case，一个源对象一个 `.rs` 文件 |
| 类型和方法 | 类型 PascalCase；方法和参数 snake_case，名称与 Java 语义一一对应 |
| 模块文件 | `lib.rs`/`mod.rs` 只含模块文档、声明和显式重导出 |
| 完成口径 | 仅 `IMPLEMENTED`、`DEPENDENCY_REUSED`、`PLATFORM_NA` 计入完成 |
| 红线 | 禁止 stub、兼容文件转引充数、生产 wildcard import、无证据的依赖替代 |
| 注释和测试 | 中文来源注释；正常、失败、边界、顺序、生命周期和并发/取消测试 |

### 当前状态快照

| 状态 | 数量 |
|---|---:|
| `DEPENDENCY_REUSED` | 0 |
| `IMPLEMENTED` | 0 |
| `MISPLACED` | 5 |
| `MISSING` | 7 |
| `PARTIAL` | 0 |
| `PLATFORM_NA` | 0 |
| `STUB` | 0 |
| `UNVERIFIED` | 0 |

### 本文档执行责任

- 本模块必须覆盖：候选组件索引、stereotype 元数据、编译期扫描、索引格式、合并与稳定排序、运行时加载。
- 必须记录入口、协作对象、回调顺序、错误传播、资源释放以及异步取消/背压语义。
- 接口相似不能代替行为证据；每个关键语义域都要有正常、失败和边界测试。
<!-- current-migration-contract-end -->

| Spring 调用链 | Rust 目标 | 当前判断 | 测试证据要求 |
|---|---|---|---|
| `init → MetadataStore.readMetadata` | 读取上一轮索引 | `MISSING` | 无文件、损坏、重复项 |
| `process → MetadataCollector.processing` | 标记本轮源类型 | `MISSING` | 多轮/增量编译 |
| `processElement → StereotypesProvider` | 提取 stereotype | `MISSING` | 标准、自定义、package |
| `MetadataCollector.add/getMetadata` | 合并当前与历史条目 | `MISSING` | 删除、变更、未处理类型 |
| `MetadataStore.writeMetadata` | 原子写出索引资源 | `MISSING` | IO 失败、覆盖策略 |
| `PropertiesMarshaller.write/read` | 确定性 properties 格式 | `MISPLACED` | 排序、转义、往返 |
| 运行时 candidate query | `LinkedComponentIndex` | `PARTIAL` | stereotype 查询与生成物集成 |

```mermaid
sequenceDiagram
    participant P as Processor
    participant C as MetadataCollector
    participant S as MetadataStore
    participant M as PropertiesMarshaller
    P->>S: read previous metadata
    P->>C: processing(round)
    P->>C: add(ItemMetadata)
    C-->>P: merged metadata
    P->>S: write metadata
    S->>M: deterministic serialization
```

当前 5 个 `index/` 文件具有部分数据/序列化候选能力，但路径错位且缺少完整 processor 链，
因此不得标为实现完成。

<!-- detailed-current-start -->
## vernal-context-indexer 语义边界

语义等价以可观察行为为准，但不能借“功能相似”消除源对象边界。

| 语义域 | Rust 目标表达 | 必须保留 | 验收证据 |
|---|---|---|---|
| 候选组件索引 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| stereotype 元数据 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 编译期扫描 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 索引格式 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 合并与稳定排序 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |
| 运行时加载 | trait/struct + `Result`/显式上下文；流式边界使用 `Stream` | 正常、边界、错误、顺序与资源释放 | 单元测试和跨对象集成测试 |

## 对象簇职责

| 目标目录 | 数量 | 代表对象 | 当前状态 |
|---|---:|---|---|
| `processor` | 12 | `CandidateComponentsIndexer`、`CandidateComponentsMetadata`、`IndexedStereotypesProvider`、`ItemMetadata`、`MetadataCollector` | MISPLACED:5、MISSING:7 |

## 目标调用链

```mermaid
flowchart LR
    A["候选组件索引"] --> B["stereotype 元数据"]
    B --> C["编译期扫描"]
    C --> D["索引格式"]
    D --> E["合并与稳定排序"]
    E --> F["结果/错误/释放"]
    F -. 失败证据 .-> G["台账状态降级"]
```

入口、适配、执行、回调和清理都必须可观察；只验证最终返回值不足以证明顺序、取消或释放正确。

## Java 到 Rust 技术语义

| Java 机制 | Rust 映射 | 验证重点 |
|---|---|---|
| nullable | `Option<T>` | `None` 与空值不得混同 |
| checked exception | `thiserror` + `Result<T, E>` | 分类、cause 和上下文 |
| synchronized/并发容器 | `Arc<RwLock<_>>`/`DashMap` | 竞争、可见性和死锁 |
| CompletableFuture | `Future`/`JoinHandle` | 完成、失败、取消和超时 |
| Reactor Mono | `async fn -> Result<T, E>` | 单值、空结果和错误 |
| Reactor Flux | `Stream<Item=Result<T,E>>` | 背压、顺序、取消和终止 |
| ServiceLoader | `inventory` + registry | 自动注册和确定性 |
| JVM/字节码 | 有证据才可 `PLATFORM_NA` | 困难不等于不适用 |

## 语义测试矩阵

| 语义域 | 正常场景 | 失败/边界 | 观察点 |
|---|---|---|---|
| 候选组件索引 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| stereotype 元数据 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 编译期扫描 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 索引格式 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 合并与稳定排序 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |
| 运行时加载 | 最小有效输入完成全链路 | 空输入、重复、下游错误或取消 | 返回值、错误类型、调用顺序、状态和释放 |

## 语义完成清单

- [ ] 对象职责和协作边界明确。
- [ ] Javadoc 前置、后置和异常语义译为中文注释。
- [ ] 关键链路有正常、失败和边界测试。
- [ ] 异步能力覆盖完成、错误、取消、超时与背压。
- [ ] 资源型对象覆盖获取、复用和释放。
- [ ] 依赖复用有固定提交、精确符号和集成测试。
- [ ] JVM 专属能力有不可迁移证据。
- [ ] 状态严格使用验收规范定义。
<!-- detailed-current-end -->
