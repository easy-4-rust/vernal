//! property_matches — 对应 Java 类：org.springframework.beans.PropertyMatches。
//!
//! 对应 Spring beans 包。

use std::any::Any;
use std::sync::Arc;

/// PropertyMatches — Spring 组件。
#[derive(Debug, Clone, Default)]
pub struct PropertyMatches {
    // TODO: 添加字段
}

impl PropertyMatches {
    pub fn new() -> Self { Self::default() }
}
