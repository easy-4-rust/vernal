//! 支撑模块: mutable_sort_definition。
use std::any::Any;
use std::sync::Arc;

/// MutableSortDefinition — 对应 Spring support 组件。
#[derive(Debug, Clone, Default)]
pub struct MutableSortDefinition {
    // TODO: 添加字段
}

impl MutableSortDefinition {
    pub fn new() -> Self { Self::default() }
}
