//! 支撑模块: property_comparator。
use std::any::Any;
use std::sync::Arc;

/// PropertyComparator — 对应 Spring support 组件。
#[derive(Debug, Clone, Default)]
pub struct PropertyComparator {
    // TODO: 添加字段
}

impl PropertyComparator {
    pub fn new() -> Self { Self::default() }
}
