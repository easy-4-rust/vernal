//! 支撑模块: sort_definition。
use std::any::Any;
use std::sync::Arc;

/// SortDefinition — 对应 Spring support 组件。
#[derive(Debug, Clone, Default)]
pub struct SortDefinition {
    // TODO: 添加字段
}

impl SortDefinition {
    pub fn new() -> Self { Self::default() }
}
