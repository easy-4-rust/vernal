//! description_utils — 对应 Java 类或 Spring 组件。
use std::any::Any;
use std::sync::Arc;

/// DescriptionUtils — 对应 Spring 组件。
#[derive(Debug, Clone, Default)]
pub struct DescriptionUtils {
    // TODO: 添加字段
}

impl DescriptionUtils {
    pub fn new() -> Self { Self::default() }
}
