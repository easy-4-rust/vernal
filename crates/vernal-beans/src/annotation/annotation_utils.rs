//! 注解模块: annotation_utils。
use std::any::Any;
use std::sync::Arc;

/// AnnotationUtils — 对应 Spring 注解组件。
#[derive(Debug, Clone, Default)]
pub struct AnnotationUtils {
    // TODO: 添加字段
}

impl AnnotationUtils {
    pub fn new() -> Self { Self::default() }
}
