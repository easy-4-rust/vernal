//! 注解模块: class_path_beans_definition_scanner。
use std::any::Any;
use std::sync::Arc;

/// ClassPathBeansDefinitionScanner — 对应 Spring 注解组件。
#[derive(Debug, Clone, Default)]
pub struct ClassPathBeansDefinitionScanner {
    // TODO: 添加字段
}

impl ClassPathBeansDefinitionScanner {
    pub fn new() -> Self { Self::default() }
}
