//! XML 解析模块: document_loader。
use std::any::Any;
use std::sync::Arc;

/// DocumentLoader — 对应 Spring XML 解析组件。
#[derive(Debug, Clone, Default)]
pub struct DocumentLoader {
    // TODO: 添加字段
}

impl DocumentLoader {
    pub fn new() -> Self { Self::default() }
}
