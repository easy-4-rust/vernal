//! XML 解析模块: resource_entity_resolver。
use std::any::Any;
use std::sync::Arc;

/// ResourceEntityResolver — 对应 Spring XML 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ResourceEntityResolver {
    // TODO: 添加字段
}

impl ResourceEntityResolver {
    pub fn new() -> Self { Self::default() }
}
