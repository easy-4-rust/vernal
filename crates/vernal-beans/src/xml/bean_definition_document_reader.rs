//! XML 解析模块: bean_definition_document_reader。
use std::any::Any;
use std::sync::Arc;

/// BeanDefinitionDocumentReader — 对应 Spring XML 解析组件。
#[derive(Debug, Clone, Default)]
pub struct BeanDefinitionDocumentReader {
    // TODO: 添加字段
}

impl BeanDefinitionDocumentReader {
    pub fn new() -> Self { Self::default() }
}
