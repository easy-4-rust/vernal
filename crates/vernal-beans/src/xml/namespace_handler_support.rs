//! XML 解析模块: namespace_handler_support。
use std::any::Any;
use std::sync::Arc;

/// NamespaceHandlerSupport — 对应 Spring XML 解析组件。
#[derive(Debug, Clone, Default)]
pub struct NamespaceHandlerSupport {
    // TODO: 添加字段
}

impl NamespaceHandlerSupport {
    pub fn new() -> Self { Self::default() }
}
