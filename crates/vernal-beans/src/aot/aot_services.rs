//! AOT 模块: aot_services。
use std::any::Any;
use std::sync::Arc;

/// AotServices — 对应 Spring AOT 组件。
#[derive(Debug, Clone, Default)]
pub struct AotServices {
    // TODO: 添加字段
}

impl AotServices {
    pub fn new() -> Self { Self::default() }
}
