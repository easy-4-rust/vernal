//! AOT 模块: bean_definition_property_value_code_generator_delegates。
use std::any::Any;
use std::sync::Arc;

/// BeanDefinitionPropertyValueCodeGeneratorDelegates — 对应 Spring AOT 组件。
#[derive(Debug, Clone, Default)]
pub struct BeanDefinitionPropertyValueCodeGeneratorDelegates {
    // TODO: 添加字段
}

impl BeanDefinitionPropertyValueCodeGeneratorDelegates {
    pub fn new() -> Self { Self::default() }
}
