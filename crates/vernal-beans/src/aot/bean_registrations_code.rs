//! AOT 模块: bean_registrations_code。
use std::any::Any;
use std::sync::Arc;

/// BeanRegistrationsCode — 对应 Spring AOT 组件。
#[derive(Debug, Clone, Default)]
pub struct BeanRegistrationsCode {
    // TODO: 添加字段
}

impl BeanRegistrationsCode {
    pub fn new() -> Self { Self::default() }
}
