//! AOT 模块: bean_definition_method_generator。
use std::any::Any;
use std::sync::Arc;

/// BeanDefinitionMethodGenerator — 对应 Spring AOT 组件。
#[derive(Debug, Clone, Default)]
pub struct BeanDefinitionMethodGenerator {
    // TODO: 添加字段
}

impl BeanDefinitionMethodGenerator {
    pub fn new() -> Self { Self::default() }
}
