//! AOT 模块: bean_definition_properties_code_generator。
use std::any::Any;
use std::sync::Arc;

/// BeanDefinitionPropertiesCodeGenerator — 对应 Spring AOT 组件。
#[derive(Debug, Clone, Default)]
pub struct BeanDefinitionPropertiesCodeGenerator {
    // TODO: 添加字段
}

impl BeanDefinitionPropertiesCodeGenerator {
    pub fn new() -> Self { Self::default() }
}
