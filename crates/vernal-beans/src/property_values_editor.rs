//! property_values_editor — 对应 Java 类或 Spring 组件。
use std::any::Any;
use std::sync::Arc;

/// PropertyValuesEditor — 对应 Spring 组件。
#[derive(Debug, Clone, Default)]
pub struct PropertyValuesEditor {
    // TODO: 添加字段
}

impl PropertyValuesEditor {
    pub fn new() -> Self { Self::default() }
}
