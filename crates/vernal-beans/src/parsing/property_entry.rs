//! 解析模块: property_entry。
use std::any::Any;
use std::sync::Arc;

/// PropertyEntry — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct PropertyEntry {
    // TODO: 添加字段
}

impl PropertyEntry {
    pub fn new() -> Self { Self::default() }
}
