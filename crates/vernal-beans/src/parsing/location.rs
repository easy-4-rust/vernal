//! 解析模块: location。
use std::any::Any;
use std::sync::Arc;

/// Location — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct Location {
    // TODO: 添加字段
}

impl Location {
    pub fn new() -> Self { Self::default() }
}
