//! 解析模块: alias_definition。
use std::any::Any;
use std::sync::Arc;

/// AliasDefinition — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct AliasDefinition {
    // TODO: 添加字段
}

impl AliasDefinition {
    pub fn new() -> Self { Self::default() }
}
