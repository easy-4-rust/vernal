//! 解析模块: import_definition。
use std::any::Any;
use std::sync::Arc;

/// ImportDefinition — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ImportDefinition {
    // TODO: 添加字段
}

impl ImportDefinition {
    pub fn new() -> Self { Self::default() }
}
