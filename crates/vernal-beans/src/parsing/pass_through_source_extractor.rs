//! 解析模块: pass_through_source_extractor。
use std::any::Any;
use std::sync::Arc;

/// PassThroughSourceExtractor — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct PassThroughSourceExtractor {
    // TODO: 添加字段
}

impl PassThroughSourceExtractor {
    pub fn new() -> Self { Self::default() }
}
