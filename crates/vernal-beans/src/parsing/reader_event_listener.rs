//! 解析模块: reader_event_listener。
use std::any::Any;
use std::sync::Arc;

/// ReaderEventListener — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ReaderEventListener {
    // TODO: 添加字段
}

impl ReaderEventListener {
    pub fn new() -> Self { Self::default() }
}
