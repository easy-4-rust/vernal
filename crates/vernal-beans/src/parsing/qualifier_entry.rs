//! 解析模块: qualifier_entry。
use std::any::Any;
use std::sync::Arc;

/// QualifierEntry — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct QualifierEntry {
    // TODO: 添加字段
}

impl QualifierEntry {
    pub fn new() -> Self { Self::default() }
}
