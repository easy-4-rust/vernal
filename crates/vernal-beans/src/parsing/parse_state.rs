//! 解析模块: parse_state。
use std::any::Any;
use std::sync::Arc;

/// ParseState — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ParseState {
    // TODO: 添加字段
}

impl ParseState {
    pub fn new() -> Self { Self::default() }
}
