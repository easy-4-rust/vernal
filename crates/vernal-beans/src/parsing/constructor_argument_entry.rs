//! 解析模块: constructor_argument_entry。
use std::any::Any;
use std::sync::Arc;

/// ConstructorArgumentEntry — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ConstructorArgumentEntry {
    // TODO: 添加字段
}

impl ConstructorArgumentEntry {
    pub fn new() -> Self { Self::default() }
}
