//! 解析模块: component_definition。
use std::any::Any;
use std::sync::Arc;

/// ComponentDefinition — 对应 Spring 解析组件。
#[derive(Debug, Clone, Default)]
pub struct ComponentDefinition {
    // TODO: 添加字段
}

impl ComponentDefinition {
    pub fn new() -> Self { Self::default() }
}
