//! Groovy 模块: groovy_bean_definition_wrapper。
use std::any::Any;
use std::sync::Arc;

/// GroovyBeanDefinitionWrapper — 对应 Spring Groovy 组件。
#[derive(Debug, Clone, Default)]
pub struct GroovyBeanDefinitionWrapper {
    // TODO: 添加字段
}

impl GroovyBeanDefinitionWrapper {
    pub fn new() -> Self { Self::default() }
}
