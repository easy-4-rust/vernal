//! Groovy 模块: groovy_bean_definition_reader。
use std::any::Any;
use std::sync::Arc;

/// GroovyBeanDefinitionReader — 对应 Spring Groovy 组件。
#[derive(Debug, Clone, Default)]
pub struct GroovyBeanDefinitionReader {
    // TODO: 添加字段
}

impl GroovyBeanDefinitionReader {
    pub fn new() -> Self { Self::default() }
}
